# ⚙️ 技术选型与架构决策

**版本**: v1.0  
**制定日期**: 2025-11-12  
**文档类型**: 技术决策文档 (ADR)

---

## 📋 目录

- [前端技术栈](#前端技术栈)
- [后端技术栈](#后端技术栈)
- [数据库选型](#数据库选型)
- [AI/ML技术](#aiml技术)
- [部署方案](#部署方案)
- [第三方服务](#第三方服务)

---

## 🎨 前端技术栈

### 核心框架选型

#### 选项对比

| 方案 | 优势 | 劣势 | 评分 |
|------|------|------|------|
| **React** | 生态丰富、社区活跃、灵活性高 | 需要较多配置、学习曲线 | ⭐⭐⭐⭐⭐ |
| Vue 3 | 易学易用、中文文档好、渐进式 | 生态相对较小 | ⭐⭐⭐⭐ |
| Svelte | 性能优秀、代码简洁 | 生态较新、资源较少 | ⭐⭐⭐ |
| Angular | 完整框架、企业级 | 学习曲线陡峭、较重 | ⭐⭐⭐ |

**最终选择**: **React 18**

**决策理由**:
```
✅ 最大的生态系统和社区支持
✅ 丰富的第三方组件库
✅ Concurrent Mode支持更好的用户体验
✅ 团队熟悉度高
✅ 适合构建复杂交互的学习平台
```

### UI组件库

| 方案 | 特点 | 适用场景 | 推荐度 |
|------|------|----------|--------|
| **Ant Design** | 企业级、组件丰富、中文文档 | 后台系统、管理平台 | ⭐⭐⭐⭐⭐ |
| Material-UI | Material Design、国际化 | 现代化应用 | ⭐⭐⭐⭐ |
| Chakra UI | 简洁、可访问性好 | 快速开发 | ⭐⭐⭐⭐ |
| Tailwind CSS | 工具类、高度定制 | 独特设计 | ⭐⭐⭐⭐ |

**最终选择**: **Ant Design + Tailwind CSS**

**决策理由**:
```
✅ Ant Design: 提供完整的企业级组件
✅ Tailwind CSS: 快速实现自定义样式
✅ 两者结合: 既有开箱即用的组件，又有灵活的定制能力
```

### 状态管理

```
技术选型对比
┌──────────────┬────────────┬────────────┬──────────┐
│     方案     │   复杂度   │   性能     │  推荐度  │
├──────────────┼────────────┼────────────┼──────────┤
│ Redux Toolkit│  中等      │   良好     │ ⭐⭐⭐⭐⭐│
│ Zustand      │  简单      │   优秀     │ ⭐⭐⭐⭐  │
│ Recoil       │  中等      │   优秀     │ ⭐⭐⭐⭐  │
│ MobX         │  简单      │   良好     │ ⭐⭐⭐    │
│ Context API  │  简单      │   中等     │ ⭐⭐⭐    │
└──────────────┴────────────┴────────────┴──────────┘
```

**最终选择**: **Redux Toolkit + React Query**

**决策理由**:
- **Redux Toolkit**: 全局状态管理（用户信息、主题设置等）
- **React Query**: 服务端状态管理（数据获取、缓存）

```typescript
// Redux Toolkit示例
import { createSlice } from '@reduxjs/toolkit';

const userSlice = createSlice({
  name: 'user',
  initialState: { info: null, token: null },
  reducers: {
    setUser: (state, action) => {
      state.info = action.payload;
    },
    setToken: (state, action) => {
      state.token = action.payload;
    },
    logout: (state) => {
      state.info = null;
      state.token = null;
    }
  }
});

// React Query示例
import { useQuery } from '@tanstack/react-query';

function useChapters(bookId) {
  return useQuery({
    queryKey: ['chapters', bookId],
    queryFn: () => fetchChapters(bookId),
    staleTime: 5 * 60 * 1000, // 5分钟
    cacheTime: 30 * 60 * 1000  // 30分钟
  });
}
```

### 代码编辑器

| 方案 | 特点 | 性能 | 推荐度 |
|------|------|------|--------|
| **Monaco Editor** | VSCode同款、功能完整 | 优秀 | ⭐⭐⭐⭐⭐ |
| CodeMirror 6 | 轻量、可扩展性强 | 优秀 | ⭐⭐⭐⭐ |
| Ace Editor | 老牌编辑器、稳定 | 良好 | ⭐⭐⭐ |

**最终选择**: **Monaco Editor**

**决策理由**:
```
✅ VSCode同款引擎，功能最全
✅ 智能提示、代码补全、错误检查
✅ 支持多种语言
✅ 主题丰富
✅ 社区活跃
```

### 图表可视化

**最终选择**: **Apache ECharts + Chart.js**

**决策理由**:
- **ECharts**: 复杂图表（雷达图、关系图、热力图）
- **Chart.js**: 简单图表（折线图、柱状图）

```javascript
// ECharts示例 - 知识点雷达图
const option = {
  radar: {
    indicator: [
      { name: '流体力学', max: 100 },
      { name: '水力计算', max: 100 },
      { name: '管道设计', max: 100 },
      { name: '泵站设计', max: 100 },
      { name: '控制理论', max: 100 }
    ]
  },
  series: [{
    type: 'radar',
    data: [{
      value: [85, 70, 90, 65, 75],
      name: '掌握程度'
    }]
  }]
};
```

### 构建工具

**最终选择**: **Vite**

**vs Webpack对比**:
```
特性          │ Vite      │ Webpack
─────────────┼───────────┼─────────
开发服务器启动│ <1s       │ 10-30s
热更新速度    │ 即时      │ 1-3s
生产构建速度  │ 快        │ 较慢
配置复杂度    │ 简单      │ 复杂
生态成熟度    │ 成长中    │ 非常成熟

推荐度        │ ⭐⭐⭐⭐⭐ │ ⭐⭐⭐⭐
```

---

## 🔧 后端技术栈

### Web框架

| 方案 | 性能 | 异步支持 | 学习曲线 | 推荐度 |
|------|------|----------|----------|--------|
| **FastAPI** | 优秀 | 原生async/await | 低 | ⭐⭐⭐⭐⭐ |
| Django | 良好 | 有限 | 中等 | ⭐⭐⭐⭐ |
| Flask | 良好 | 扩展支持 | 低 | ⭐⭐⭐ |
| Tornado | 优秀 | 优秀 | 高 | ⭐⭐⭐ |

**最终选择**: **FastAPI**

**决策理由**:
```
✅ 性能优异 (接近Go/Node.js)
✅ 原生异步支持
✅ 自动API文档 (OpenAPI/Swagger)
✅ 类型提示和验证 (Pydantic)
✅ 现代Python特性 (3.7+)
✅ 易于测试
```

```python
# FastAPI示例
from fastapi import FastAPI, Depends
from pydantic import BaseModel

app = FastAPI()

class Chapter(BaseModel):
    title: str
    content: str
    order: int

@app.post("/api/v1/chapters")
async def create_chapter(
    chapter: Chapter,
    current_user: User = Depends(get_current_user)
):
    """创建章节"""
    new_chapter = await Chapter.create(**chapter.dict())
    return {"success": True, "data": new_chapter}
```

### ORM选型

| 方案 | 异步支持 | 性能 | 易用性 | 推荐度 |
|------|----------|------|--------|--------|
| **SQLAlchemy 2.0** | ✅ | 优秀 | 中等 | ⭐⭐⭐⭐⭐ |
| Tortoise ORM | ✅ | 优秀 | 简单 | ⭐⭐⭐⭐ |
| Django ORM | 部分 | 良好 | 简单 | ⭐⭐⭐ |

**最终选择**: **SQLAlchemy 2.0**

**决策理由**:
```
✅ 最成熟的Python ORM
✅ SQLAlchemy 2.0原生支持async
✅ 强大的查询能力
✅ 灵活的模型定义
✅ 完善的迁移工具 (Alembic)
```

```python
# SQLAlchemy 2.0异步示例
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession
from sqlalchemy.orm import selectinload

engine = create_async_engine("postgresql+asyncpg://...")

async def get_user_with_progress(user_id: int):
    async with AsyncSession(engine) as session:
        stmt = (
            select(User)
            .options(selectinload(User.learning_progress))
            .where(User.id == user_id)
        )
        result = await session.execute(stmt)
        return result.scalar_one()
```

### 任务队列

| 方案 | 功能 | 性能 | 易用性 | 推荐度 |
|------|------|------|--------|--------|
| **Celery** | 丰富 | 良好 | 中等 | ⭐⭐⭐⭐⭐ |
| RQ | 简单 | 良好 | 简单 | ⭐⭐⭐⭐ |
| Dramatiq | 现代 | 优秀 | 中等 | ⭐⭐⭐⭐ |

**最终选择**: **Celery + Redis**

**适用场景**:
- 异步任务: 发送邮件、生成报告
- 定时任务: 数据统计、备份
- 长时间任务: AI模型训练、大数据处理

```python
# Celery任务示例
from celery import Celery

celery_app = Celery('platform', broker='redis://localhost:6379/0')

@celery_app.task
async def generate_learning_report(user_id: int):
    """生成学习报告 (异步任务)"""
    # 1. 收集数据
    data = await collect_learning_data(user_id)
    
    # 2. 生成报告
    report = await generate_pdf_report(data)
    
    # 3. 发送邮件
    await send_email(user_id, report)
    
    return {"success": True, "report_id": report.id}
```

### API文档

**最终选择**: **OpenAPI (Swagger) + ReDoc**

FastAPI自动生成，无需额外配置：

```python
app = FastAPI(
    title="Platform API",
    description="智能学习平台API文档",
    version="1.0.0",
    docs_url="/docs",      # Swagger UI
    redoc_url="/redoc"     # ReDoc
)
```

---

## 💾 数据库选型

### 关系型数据库

| 方案 | 性能 | 功能 | 社区 | 推荐度 |
|------|------|------|------|--------|
| **PostgreSQL** | 优秀 | 丰富 | 活跃 | ⭐⭐⭐⭐⭐ |
| MySQL | 良好 | 良好 | 活跃 | ⭐⭐⭐⭐ |
| SQLite | 轻量 | 基础 | 活跃 | ⭐⭐⭐ |

**最终选择**: **PostgreSQL 15**

**决策理由**:
```
✅ 功能最丰富 (JSONB、全文搜索、GIS)
✅ 性能优异
✅ 数据完整性保障
✅ 支持复杂查询
✅ 扩展性强 (pg_vector等)
✅ 开源免费
```

**特色功能应用**:
```sql
-- JSONB存储灵活数据
CREATE TABLE user_preferences (
    user_id BIGINT PRIMARY KEY,
    preferences JSONB
);

-- JSONB查询
SELECT * FROM user_preferences 
WHERE preferences @> '{"theme": "dark"}';

-- 全文搜索
CREATE INDEX idx_chapters_content ON chapters 
USING GIN (to_tsvector('chinese', content));

SELECT * FROM chapters 
WHERE to_tsvector('chinese', content) @@ to_tsquery('chinese', '水力学');
```

### NoSQL数据库

**最终选择**: **MongoDB**

**使用场景**:
- 教材内容存储 (Markdown、富文本)
- 日志存储
- 用户行为数据
- 临时数据缓存

```python
# MongoDB示例
from motor.motor_asyncio import AsyncIOMotorClient

client = AsyncIOMotorClient("mongodb://localhost:27017")
db = client.platform

async def save_chapter_content(chapter_id: int, content: dict):
    """保存章节内容"""
    await db.chapters.update_one(
        {"chapter_id": chapter_id},
        {"$set": content},
        upsert=True
    )
```

### 缓存

**最终选择**: **Redis**

**使用场景**:
```
┌─────────────────────────────────────────┐
│         Redis应用场景                   │
├─────────────────────────────────────────┤
│                                         │
│  1. 会话缓存                             │
│     - 用户Token                         │
│     - 登录状态                          │
│                                         │
│  2. 数据缓存                             │
│     - 热门内容                          │
│     - 用户信息                          │
│     - 查询结果                          │
│                                         │
│  3. 计数器                               │
│     - 访问统计                          │
│     - 点赞数                            │
│                                         │
│  4. 排行榜                               │
│     - 学习时长排行                      │
│     - 积分排行                          │
│                                         │
│  5. 消息队列                             │
│     - Celery broker                     │
│     - 实时通知                          │
│                                         │
│  6. 分布式锁                             │
│     - 防止重复提交                      │
│     - 限流控制                          │
│                                         │
└─────────────────────────────────────────┘
```

### 向量数据库

**最终选择**: **Chroma**

**vs 其他方案**:
| 方案 | 易用性 | 性能 | 成本 | 推荐度 |
|------|--------|------|------|--------|
| **Chroma** | 简单 | 良好 | 免费 | ⭐⭐⭐⭐⭐ |
| Pinecone | 简单 | 优秀 | 付费 | ⭐⭐⭐⭐ |
| Weaviate | 中等 | 优秀 | 免费 | ⭐⭐⭐⭐ |
| Milvus | 复杂 | 优秀 | 免费 | ⭐⭐⭐ |

**使用场景**: 
- RAG (检索增强生成)
- 语义搜索
- 相似内容推荐

```python
# Chroma示例
from langchain.vectorstores import Chroma
from langchain.embeddings import OpenAIEmbeddings

# 创建向量数据库
embeddings = OpenAIEmbeddings()
vectorstore = Chroma(
    embedding_function=embeddings,
    persist_directory="./chroma_db"
)

# 添加文档
vectorstore.add_texts(
    texts=["章节内容1", "章节内容2"],
    metadatas=[{"chapter_id": 1}, {"chapter_id": 2}]
)

# 相似度搜索
results = vectorstore.similarity_search("水力学基础", k=5)
```

---

## 🤖 AI/ML技术

### 大语言模型

| 方案 | 性能 | 成本 | 部署 | 推荐度 |
|------|------|------|------|--------|
| **GPT-4** | 顶级 | 高 | API | ⭐⭐⭐⭐⭐ |
| GPT-3.5 | 优秀 | 中 | API | ⭐⭐⭐⭐ |
| Claude 2 | 优秀 | 中 | API | ⭐⭐⭐⭐ |
| Llama 2 | 良好 | 免费 | 自部署 | ⭐⭐⭐ |

**策略**: **混合使用**

```python
class LLMRouter:
    """LLM路由器 - 根据任务选择合适的模型"""
    
    def route(self, task_type: str, complexity: str):
        if task_type == 'code_analysis' and complexity == 'simple':
            # 简单任务用本地模型
            return LocalCodeAnalyzer()
        
        elif task_type == 'qa' and complexity == 'simple':
            # 简单问答用GPT-3.5
            return GPT35Model()
        
        elif task_type == 'qa' and complexity == 'complex':
            # 复杂问答用GPT-4
            return GPT4Model()
        
        elif task_type == 'generation':
            # 内容生成用Claude
            return ClaudeModel()
```

### 推荐算法

**最终选择**: **LightGBM + 深度学习**

```
推荐系统架构
┌──────────────────────────────────────┐
│         召回层 (Recall)              │
│  ┌────────┐  ┌────────┐  ┌────────┐│
│  │协同过滤│  │内容推荐│  │知识图谱││
│  └────────┘  └────────┘  └────────┘│
└──────────────────────────────────────┘
              ▼
┌──────────────────────────────────────┐
│         排序层 (Ranking)             │
│  ┌──────────────────────────────┐  │
│  │   LightGBM / DeepFM          │  │
│  │   特征工程 + 模型融合         │  │
│  └──────────────────────────────┘  │
└──────────────────────────────────────┘
              ▼
┌──────────────────────────────────────┐
│         重排层 (Re-ranking)          │
│  - 多样性处理                        │
│  - 业务规则                          │
│  - A/B测试                           │
└──────────────────────────────────────┘
```

### Embeddings模型

**最终选择**: 
- **文本**: `text-embedding-ada-002` (OpenAI)
- **代码**: `code-search-ada-code-001` (OpenAI)

**备选**: 
- `sentence-transformers` (本地部署)

---

## 🚀 部署方案

### 容器化

**最终选择**: **Docker + Docker Compose**

```yaml
# docker-compose.yml
version: '3.8'

services:
  # 前端
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
  
  # 后端
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://...
      - REDIS_URL=redis://redis:6379
    depends_on:
      - postgres
      - redis
      - mongodb
  
  # PostgreSQL
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: platform
      POSTGRES_USER: platform
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
  
  # Redis
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
  
  # MongoDB
  mongodb:
    image: mongo:6
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
  
  # Celery Worker
  celery_worker:
    build: ./backend
    command: celery -A app.celery worker --loglevel=info
    depends_on:
      - redis
      - backend

volumes:
  postgres_data:
  mongo_data:
```

### 编排方案

| 方案 | 适用规模 | 复杂度 | 推荐度 |
|------|----------|--------|--------|
| **Docker Compose** | 小型 | 低 | ⭐⭐⭐⭐⭐ |
| Kubernetes | 大型 | 高 | ⭐⭐⭐⭐ |
| Docker Swarm | 中型 | 中 | ⭐⭐⭐ |

**推荐路径**:
```
V1.0: Docker Compose  →  V2.0: Kubernetes
(快速上线)                (扩展性强)
```

### CI/CD

**最终选择**: **GitHub Actions**

```yaml
# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Run Tests
        run: |
          cd backend
          pip install -r requirements.txt
          pytest
      
      - name: Build Docker Images
        run: |
          docker-compose build
      
      - name: Deploy to Server
        run: |
          ssh user@server 'cd /app && docker-compose pull && docker-compose up -d'
```

### 监控方案

**最终选择**: **Prometheus + Grafana**

```python
# FastAPI集成Prometheus
from prometheus_fastapi_instrumentator import Instrumentator

app = FastAPI()

Instrumentator().instrument(app).expose(app)
```

---

## 🌐 第三方服务

### 对象存储

**最终选择**: **阿里云OSS / AWS S3**

**使用场景**:
- 用户头像
- 教材附件
- 视频资源
- 备份文件

### CDN

**最终选择**: **Cloudflare**

**优势**:
```
✅ 免费额度足够
✅ 全球节点
✅ DDoS防护
✅ SSL证书
✅ 缓存优化
```

### 邮件服务

**最终选择**: **SendGrid / 阿里云邮件推送**

### 短信服务

**最终选择**: **阿里云短信 / 腾讯云短信**

### 支付

**最终选择**: **微信支付 + 支付宝**

---

## 📊 技术栈总览

```
┌─────────────────────────────────────────────┐
│              Platform技术栈                  │
├─────────────────────────────────────────────┤
│                                             │
│  前端层                                      │
│  ├─ React 18 + TypeScript                  │
│  ├─ Redux Toolkit + React Query            │
│  ├─ Ant Design + Tailwind CSS              │
│  ├─ Monaco Editor                           │
│  └─ ECharts + Chart.js                      │
│                                             │
│  后端层                                      │
│  ├─ FastAPI + Python 3.12                  │
│  ├─ SQLAlchemy 2.0 (async)                 │
│  ├─ Celery + Redis                          │
│  └─ Pydantic                                │
│                                             │
│  数据层                                      │
│  ├─ PostgreSQL 15                           │
│  ├─ MongoDB 6                               │
│  ├─ Redis 7                                 │
│  └─ Chroma (向量数据库)                     │
│                                             │
│  AI层                                        │
│  ├─ GPT-4 / GPT-3.5                         │
│  ├─ LangChain                               │
│  ├─ OpenAI Embeddings                       │
│  └─ LightGBM                                │
│                                             │
│  部署层                                      │
│  ├─ Docker + Docker Compose                │
│  ├─ Nginx                                   │
│  ├─ GitHub Actions                          │
│  └─ Prometheus + Grafana                    │
│                                             │
│  第三方服务                                  │
│  ├─ 阿里云OSS (对象存储)                    │
│  ├─ Cloudflare (CDN)                        │
│  ├─ SendGrid (邮件)                         │
│  └─ 微信支付 + 支付宝                        │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 🎯 版本演进计划

```
V1.0 (当前)           V1.1 (3个月)         V2.0 (6个月)
────────────────────  ──────────────────  ───────────────────
✅ 单体架构           → 优化架构          → 微服务架构
✅ Docker Compose     ✅ 保持             → Kubernetes
✅ PostgreSQL         ✅ 保持 + 读写分离  ✅ 分库分表
✅ Redis单机          → Redis Cluster     ✅ 保持
✅ OpenAI API         ✅ 混合策略         → 自部署模型
✅ 手动部署           → CI/CD自动化       ✅ 保持
```

---

**文档版本**: v1.0  
**最后更新**: 2025-11-12  
**下次审查**: 2026-02-12
