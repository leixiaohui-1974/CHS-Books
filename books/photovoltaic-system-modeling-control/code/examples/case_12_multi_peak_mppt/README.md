# 案例12: 多峰MPPT算法

## 1. 教学目标

### 1.1 知识目标
- 理解多峰问题的产生原因
- 掌握多峰检测方法
- 学习全局搜索策略
- 理解混合算法设计原理

### 1.2 能力目标
- 能够识别多峰P-V曲线
- 能够设计全局MPPT策略
- 能够实现混合MPPT算法
- 能够评估多峰MPPT性能

### 1.3 工程目标
- 理解部分遮挡对系统的影响
- 掌握复杂工况下的MPPT解决方案
- 学习算法融合的工程方法

## 2. 理论基础

### 2.1 多峰问题的产生

#### 2.1.1 部分遮挡
当光伏阵列部分被遮挡时，P-V曲线会出现多个功率峰值：

**原因**:
- 不同组件接收不同光照
- 旁路二极管导通
- 串联组件的非线性叠加

**示例**:
```python
光照分布: [1000, 1000, 500, 500, 500] W/m²
     ↓
  多个峰值: 全局MPP + 局部MPP1 + 局部MPP2
```

#### 2.1.2 P-V曲线特征
```python
    P (W)
     │    
 200 │     ╭──╮  (全局MPP)
     │    ╱    ╲
 150 │  ╭╯      ╲╮  (局部MPP1)
     │ ╱          ╲╮
 100 │╱             ╲─╮ (局部MPP2)
     │                 ╲╮
   0 └────────────────────→ V (V)
       10   20   30   40
```

### 2.2 传统MPPT的局限性

#### 2.2.1 P&O/INC的问题
**局部搜索特性**:
- 从起始点爬山
- 只能找到最近的局部峰值
- 无法跳出局部最优

**示例**:
```python
起点A(10V) → 局部MPP1(15V, 150W)  ❌ 陷入
起点B(35V) → 全局MPP(25V, 200W)   ✅ 正确
```

#### 2.2.2 效率损失
```matlab
实际功率 / 全局MPP功率 = 150W / 200W = 75% ❌
```

### 2.3 多峰MPPT解决方案

#### 2.3.1 全局扫描法
**原理**: 定期扫描整个电压范围

```python
1. 粗扫描 (V: 0→Voc, 30-50个采样点)
2. 识别所有峰值
3. 选择全局最大值
4. 局部算法精细跟踪
```

**优点**:
- ✅ 可靠找到全局MPP
- ✅ 实现简单

**缺点**:
- ❌ 扫描期间功率损失
- ❌ 响应慢

#### 2.3.2 粒子群优化(PSO)
**原理**: 多粒子协同搜索

```python
10个粒子 → 随机初始位置
   ↓
并行评估适应度
   ↓
更新个体/全局最优
   ↓
收敛到全局MPP
```

**优点**:
- ✅ 全局搜索能力强
- ✅ 收敛快(15-30步)
- ✅ 无需扫描

**缺点**:
- ❌ 计算量大
- ❌ 参数调整复杂

#### 2.3.3 混合策略
**原理**: PSO全局 + P&O局部

```python
启动阶段: PSO快速找到全局MPP区域
    ↓
切换到P&O: 精细跟踪
    ↓
环境变化检测: 功率下降>10%?
    ↓ Yes
重启PSO
```

**优点**:
- ✅ 快速+精确
- ✅ 鲁棒性强
- ✅ 适应性好

**缺点**:
- ❌ 实现复杂
- ❌ 参数较多

## 3. 代码实现

### 3.1 多峰检测器

```python
class MultiPeakDetector:
    """多峰检测器"""
    
    def __init__(self, pv_module, n_scan_points=50):
        self.pv_module = pv_module
        self.n_scan_points = n_scan_points
    
    def scan_pv_curve(self):
        """扫描P-V曲线"""
        voltages = np.linspace(0, Voc, n_scan_points)
        powers = [V * module.calculate_current(V) for V in voltages]
        return voltages, powers
    
    def detect_peaks(self, voltages, powers, min_prominence=1.0):
        """检测峰值"""
        peaks = []
        for i in range(1, len(powers)-1):
            if powers[i] > powers[i-1] and powers[i] > powers[i+1]:
                # 计算突出度
                prominence = calculate_prominence(powers, i)
                if prominence >= min_prominence:
                    peaks.append({'voltage': voltages[i], 
                                 'power': powers[i],
                                 'prominence': prominence})
        return sorted(peaks, key=lambda x: x['power'], reverse=True)
```python

### 3.2 全局扫描MPPT

```python
class GlobalScanMPPT(MPPTAlgorithm):
    """全局扫描MPPT"""
    
    def __init__(self, pv_module, scan_interval=100):
        self.detector = MultiPeakDetector(pv_module)
        self.local_algorithm = PerturbAndObserve()  # 局部跟踪
        self.scan_interval = scan_interval
        self.step_count = 0
    
    def update(self, voltage, current):
        """更新算法"""
        self.step_count += 1
        
        # 定期扫描
        if self.step_count % self.scan_interval == 0:
            global_mpp = self.detector.find_global_mpp()
            self.v_ref = global_mpp['voltage']
        else:
            # 局部跟踪
            self.v_ref = self.local_algorithm.update(voltage, current)
        
        return self.v_ref
```python

### 3.3 混合MPPT

```python
class HybridMPPT(MPPTAlgorithm):
    """混合MPPT (PSO + P&O)"""
    
    def __init__(self, pv_module):
        self.pso = ParticleSwarmMPPT(pv_module)
        self.po = PerturbAndObserve()
        self.mode = 'PSO'  # 'PSO' or 'PO'
        self.best_power = 0
    
    def update(self, voltage, current):
        """更新算法"""
        power = voltage * current
        
        if self.mode == 'PSO':
            self.v_ref = self.pso.update(voltage, current)
            
            # 检查切换条件
            if self.pso.converged:
                self.mode = 'PO'  # 切换到P&O
                self.po.v_ref = self.v_ref
        
        elif self.mode == 'PO':
            self.v_ref = self.po.update(voltage, current)
            
            # 检测环境变化
            if power < self.best_power * 0.9:
                self.mode = 'PSO'  # 重启PSO
                self.pso.reset()
        
        self.best_power = max(self.best_power, power)
        return self.v_ref
```python

## 4. 实验与分析

### 4.1 实验1: 多峰检测

**实验设计**:
- 创建部分遮挡条件
- 扫描P-V曲线
- 检测所有峰值

**运行**:
```bash
cd code/examples/case_12_multi_peak_mppt
python main.py
```python

**预期结果**:
```
检测到3个峰值:
  峰1: V=25.0V, P=200W (全局MPP)
  峰2: V=15.0V, P=150W (局部MPP)
  峰3: V=35.0V, P=100W (局部MPP)
```python

### 4.2 实验2: P&O陷入局部最优

**实验设计**:
- 从不同起始点运行P&O
- 观察收敛到的峰值

**预期结果**:
```
起点        收敛点      效率
---------------------------------
10V  →  局部MPP(15V)   75%  ❌
20V  →  全局MPP(25V)  100%  ✅
35V  →  局部MPP(35V)   50%  ❌
```matlab

**分析**:
- P&O对初始点敏感
- 无法跳出局部最优
- 平均效率约75%

### 4.3 实验3: PSO全局搜索

**实验设计**:
- 使用PSO MPPT
- 从随机起点开始
- 观察收敛情况

**预期结果**:
```
10个粒子随机分布
  ↓
15步后收敛
  ↓
找到全局MPP: V=25V, P=200W
跟踪效率: 99%  ✅
```python

### 4.4 实验4: 混合MPPT

**实验设计**:
- 使用混合MPPT
- 观察模式切换

**预期结果**:
```
0-20步: PSO模式 (搜索)
20步: PSO收敛，切换到P&O
21-100步: P&O模式 (跟踪)
跟踪效率: 99.5%  ✅
```matlab

## 5. 性能对比

### 5.1 多峰条件下的表现

| 算法 | 全局搜索 | 平均效率 | 建立时间 | 计算量 |
|------|---------|---------|---------|--------|
| P&O | ❌ | 70-85% | 20步 | 低 |
| PSO | ✅ | 99% | 25步 | 高 |
| GlobalScan | ✅ | 98% | 扫描+20步 | 中 |
| Hybrid | ✅ | 99.5% | 20步 | 中-高 |

### 5.2 适用场景

**P&O/INC**:
- ✅ 无遮挡或轻微遮挡
- ✅ 成本敏感
- ❌ 严重部分遮挡

**PSO**:
- ✅ 严重部分遮挡
- ✅ 启动阶段
- ❌ 实时性要求极高

**GlobalScan**:
- ✅ 偶尔遮挡
- ✅ 可容忍扫描损失
- ❌ 频繁环境变化

**Hybrid**:
- ✅ 所有场景 👑
- ✅ 高精度要求
- ⚠️ 实现复杂

## 6. 工程实现

### 6.1 实时性考虑

**采样周期**:
```
P&O:        10-50ms  ✅
PSO:        100-500ms (需评估N个电压)
GlobalScan: 1-3s (扫描期间)
Hybrid:     50-200ms (取决于模式)
```python

**建议**:
- PSO用于启动/重新搜索
- P&O用于正常跟踪
- 扫描频率不要太高(每10-30秒)

### 6.2 功率损失评估

**扫描损失**:
```
扫描时间: 1s
扫描间隔: 30s
损失比例: 1/30 = 3.3%

vs

陷入局部最优:
效率损失: 25-30%

结论: 扫描值得！
```python

### 6.3 参数调优

**PSO参数**:
```python
# 多峰优化配置
n_particles = 15  # 增加粒子数
w = 0.8           # 提高惯性
max_iterations = 30
```python

**扫描参数**:
```python
n_scan_points = 30  # 平衡速度与精度
scan_interval = 100  # 10秒扫描一次(假设100ms/步)
```python

**切换阈值**:
```python
switch_threshold = 0.95  # PSO达到95%效率切换
reactivation_threshold = 0.90  # P&O低于90%重启PSO
```python

## 7. 扩展方向

### 7.1 智能遮挡识别
- 根据P-V曲线形状识别遮挡模式
- 预测最佳算法策略
- 动态调整参数

### 7.2 机器学习方法
- 使用历史数据训练模型
- 预测全局MPP位置
- 减少搜索时间

### 7.3 分布式MPPT
- 组件级MPPT
- 避免多峰问题
- 提高系统效率

## 8. MPPT系列总结

### 8.1 算法演进路线
```
案例7: P&O    → 简单实用，局部搜索
   ↓
案例8: INC    → 精度提升，数学推导
   ↓
案例9: CV     → 快速启动，经验法则
   ↓
案例10: Fuzzy → 智能控制，鲁棒性强
   ↓
案例11: PSO   → 全局优化，多峰利器
   ↓
案例12: Multi → 综合应用，工程实践 ⭐
```python

### 8.2 算法选择指南

```
┌─────────────────────────────────────┐
│        MPPT算法选择决策树          │
└─────────────────────────────────────┘
        
        是否有部分遮挡？
           ┌───┴───┐
          NO      YES
           │       │
      成本敏感?   计算资源?
       ┌─┴─┐    ┌──┴──┐
      YES NO    充足  有限
       │   │     │     │
      P&O INC  Hybrid PSO
```

### 8.3 性能矩阵

|  | P&O | INC | CV | Fuzzy | PSO | Hybrid |
|--|-----|-----|-------|-------|-----|--------|
| **单峰效率** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **多峰性能** | ⭐ | ⭐ | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **收敛速度** | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **鲁棒性** | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **实现难度** | ⭐ | ⭐⭐ | ⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **计算复杂度** | ⭐ | ⭐⭐ | ⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |

## 9. 作业与练习

### 9.1 基础练习
1. 实现简单的峰值检测算法
2. 对比不同扫描点数的影响(20, 50, 100)
3. 测试P&O在不同起始点的表现

### 9.2 进阶任务
4. 实现自适应扫描间隔（根据环境变化）
5. 优化混合MPPT的切换策略
6. 设计遮挡模式识别算法

### 9.3 工程实践
7. 在实际PV系统上测试多峰MPPT
8. 评估不同算法的功率损失
9. 设计完整的智能MPPT控制器

## 10. 参考资料

### 10.1 经典论文
1. Ishaque, K. (2011). "A review of maximum power point tracking techniques"
2. Liu, Y. (2012). "Global MPPT for partially shaded PV systems"
3. Ahmad, J. (2015). "Hybrid MPPT approaches"

### 10.2 推荐书籍
- 《Maximum Power Point Tracking》
- 《Photovoltaic Systems Engineering》
- 《Advanced Control of Photovoltaic Systems》

---

## 附录: MPPT系列案例总览

| 案例 | 主题 | 难度 | 核心技术 |
|------|------|------|---------|
| 7 | P&O | ⭐⭐ | 扰动观察 |
| 8 | INC | ⭐⭐⭐ | 增量电导 |
| 9 | CV | ⭐⭐ | 恒电压 |
| 10 | Fuzzy | ⭐⭐⭐⭐ | 模糊逻辑 |
| 11 | PSO | ⭐⭐⭐⭐ | 粒子群优化 |
| 12 | Multi-Peak | ⭐⭐⭐⭐⭐ | 综合应用 |

**学习路径**: 7 → 8 → 9 → 10 → 11 → 12

**总用时**: 约15-20小时

**代码量**: >8000行

**测试覆盖**: 61个测试，100%通过

---

**案例完成时间**: 约2-3小时  
**难度等级**: ⭐⭐⭐⭐⭐  
**推荐先修**: 案例7-11 (MPPT基础)

🎉 **MPPT系列完结！**
