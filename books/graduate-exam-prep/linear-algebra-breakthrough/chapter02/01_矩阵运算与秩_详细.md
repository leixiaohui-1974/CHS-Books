# 第2章：矩阵运算与秩的快速求解

**学习时间**: 4小时  
**难度**: ⭐⭐⭐⭐  
**考试权重**: 15%  
**必考题型**: ⭐⭐⭐⭐⭐

---

## 一、矩阵基本运算

### 1.1 矩阵乘法

**定义**：$C = AB$，则
$$c_{ij} = \sum_{k=1}^{n} a_{ik}b_{kj}$$

**性质**：
- **不满足交换律**：$AB \neq BA$（一般）
- **满足结合律**：$(AB)C = A(BC)$
- **分配律**：$A(B+C) = AB + AC$
- **$(AB)^T = B^T A^T$**
- **$(AB)^{-1} = B^{-1}A^{-1}$**

### 1.2 逆矩阵

**定义**：若 $AB = BA = I$，则 $B = A^{-1}$

**存在条件**：$|A| \neq 0$（非奇异）

**求法**：

1. **伴随矩阵法**
   $$A^{-1} = \frac{1}{|A|}A^*$$
   
   其中 $A^*$ 是伴随矩阵

2. **初等行变换**
   $$[A | I] \to [I | A^{-1}]$$

3. **分块矩阵**（特殊情况）

### 1.3 矩阵的秩

**定义**：矩阵中非零子式的最高阶数

**性质**：
1. $0 \leq r(A) \leq \min(m, n)$
2. $r(A^T) = r(A)$
3. $r(AB) \leq \min(r(A), r(B))$
4. $r(A+B) \leq r(A) + r(B)$
5. **初等变换不改变秩**

---

## 二、秩的快速计算

### 2.1 阶梯形法（标准方法）

**步骤**：
1. 化为行阶梯形
2. 数非零行数

**行阶梯形**：
- 非零行在零行上方
- 非零行首非零元左边的零越来越多

### 2.2 常见秩的结论

| 矩阵类型 | 秩 |
|---------|-----|
| $A_{m \times n}$，行满秩 | $r(A) = m$ |
| $A_{m \times n}$，列满秩 | $r(A) = n$ |
| $A_n$，满秩 | $r(A) = n$ |
| $AB=O$，且A满秩 | $B=O$ |

### Python实现

```python
import numpy as np
from scipy.linalg import lu
import sympy as sp

class MatrixOperations:
    """
    矩阵运算与秩计算
    """
    
    @staticmethod
    def multiply(A, B):
        """
        矩阵乘法
        """
        if A.shape[1] != B.shape[0]:
            raise ValueError("矩阵维度不匹配")
        
        C = np.dot(A, B)
        return C
    
    @staticmethod
    def inverse_adjoint(A):
        """
        伴随矩阵法求逆
        
        A^(-1) = (1/|A|) * A*
        """
        det_A = np.linalg.det(A)
        
        if abs(det_A) < 1e-10:
            raise ValueError("矩阵奇异，不可逆")
        
        # 使用NumPy直接求逆（内部实现更优）
        A_inv = np.linalg.inv(A)
        
        return A_inv
    
    @staticmethod
    def inverse_elementary(A):
        """
        初等行变换求逆
        
        [A | I] -> [I | A^(-1)]
        """
        n = A.shape[0]
        
        # 构造增广矩阵 [A | I]
        augmented = np.hstack([A.astype(float), np.eye(n)])
        
        # 高斯-若尔当消元
        for i in range(n):
            # 主元化为1
            pivot = augmented[i, i]
            if abs(pivot) < 1e-10:
                raise ValueError("矩阵奇异")
            
            augmented[i] /= pivot
            
            # 消去其他行
            for j in range(n):
                if i != j:
                    factor = augmented[j, i]
                    augmented[j] -= factor * augmented[i]
        
        # 提取逆矩阵
        A_inv = augmented[:, n:]
        
        return A_inv
    
    @staticmethod
    def rank_echelon(A):
        """
        通过行阶梯形求秩
        
        参数:
            A: m×n矩阵
        
        返回:
            秩, 行阶梯形矩阵
        """
        A = A.astype(float).copy()
        m, n = A.shape
        
        rank = 0
        
        for col in range(n):
            # 找主元
            pivot_row = None
            for row in range(rank, m):
                if abs(A[row, col]) > 1e-10:
                    pivot_row = row
                    break
            
            if pivot_row is None:
                continue  # 这一列全为0
            
            # 交换行
            if pivot_row != rank:
                A[[rank, pivot_row]] = A[[pivot_row, rank]]
            
            # 消元
            for row in range(rank + 1, m):
                if abs(A[row, col]) > 1e-10:
                    factor = A[row, col] / A[rank, col]
                    A[row] -= factor * A[rank]
            
            rank += 1
            
            if rank == m:
                break
        
        return rank, A
    
    @staticmethod
    def rank_numpy(A):
        """
        使用NumPy计算秩
        """
        return np.linalg.matrix_rank(A)
    
    @staticmethod
    def rank_symbolic(A):
        """
        符号矩阵的秩
        """
        A_sym = sp.Matrix(A)
        return A_sym.rank()


# 示例1：矩阵乘法
print("="*60)
print("示例1：矩阵乘法")
print("="*60)

A = np.array([
    [1, 2],
    [3, 4]
])

B = np.array([
    [5, 6],
    [7, 8]
])

C = MatrixOperations.multiply(A, B)

print("A =")
print(A)
print("\nB =")
print(B)
print("\nAB =")
print(C)

# 验证：AB ≠ BA
BA = MatrixOperations.multiply(B, A)
print("\nBA =")
print(BA)
print(f"\nAB == BA? {np.allclose(C, BA)}")

# 示例2：矩阵求逆
print("\n" + "="*60)
print("示例2：矩阵求逆")
print("="*60)

A = np.array([
    [1, 2, 3],
    [0, 1, 4],
    [5, 6, 0]
])

print("矩阵A:")
print(A)

# 方法1：伴随矩阵法（NumPy实现）
A_inv1 = MatrixOperations.inverse_adjoint(A)
print("\n方法1（伴随矩阵法）:")
print(A_inv1)

# 方法2：初等行变换
A_inv2 = MatrixOperations.inverse_elementary(A)
print("\n方法2（初等行变换）:")
print(A_inv2)

# 验证：A * A^(-1) = I
I = np.dot(A, A_inv1)
print("\n验证 A * A^(-1):")
print(I)
print(f"是否为单位阵? {np.allclose(I, np.eye(3))}")

# 示例3：矩阵的秩
print("\n" + "="*60)
print("示例3：矩阵的秩")
print("="*60)

A = np.array([
    [1, 2, 3, 4],
    [2, 4, 6, 8],
    [3, 6, 9, 12],
    [1, 0, 1, 2]
])

print("矩阵A:")
print(A)

# 方法1：行阶梯形
rank1, echelon = MatrixOperations.rank_echelon(A)
print(f"\n方法1（行阶梯形）: r(A) = {rank1}")
print("行阶梯形:")
print(echelon)

# 方法2：NumPy
rank2 = MatrixOperations.rank_numpy(A)
print(f"\n方法2（NumPy）: r(A) = {rank2}")

# 示例4：符号矩阵
print("\n" + "="*60)
print("示例4：符号矩阵的秩")
print("="*60)

a = sp.Symbol('a')

A_sym = np.array([
    [1, 2, 3],
    [2, 4, 6],
    [a, 2*a, 3*a]
], dtype=object)

print("矩阵A(a):")
print(A_sym)

rank_sym = MatrixOperations.rank_symbolic(A_sym)
print(f"\nr(A) = {rank_sym}")
print("（前两行成比例，第三行也成比例，秩≤1）")
```

---

## 三、分块矩阵技巧

### 3.1 分块乘法

$$\begin{bmatrix} A_{11} & A_{12} \\ A_{21} & A_{22} \end{bmatrix} \begin{bmatrix} B_{11} & B_{12} \\ B_{21} & B_{22} \end{bmatrix} = \begin{bmatrix} A_{11}B_{11}+A_{12}B_{21} & * \\ * & * \end{bmatrix}$$

**条件**：各块维度相容

### 3.2 分块矩阵的逆

**准对角矩阵**：
$$\begin{bmatrix} A & O \\ O & B \end{bmatrix}^{-1} = \begin{bmatrix} A^{-1} & O \\ O & B^{-1} \end{bmatrix}$$

**反对角矩阵**：
$$\begin{bmatrix} O & A \\ B & O \end{bmatrix}^{-1} = \begin{bmatrix} O & B^{-1} \\ A^{-1} & O \end{bmatrix}$$

---

## 四、典型考题

### 考题1：矩阵乘法

**【题目】**已知
$$A = \begin{bmatrix} 1 & 0 \\ 0 & -1 \end{bmatrix}, \quad B = \begin{bmatrix} 0 & 1 \\ 1 & 0 \end{bmatrix}$$

求 $AB$, $BA$, $(AB)^{-1}$

**【解答】**

$$AB = \begin{bmatrix} 0 & 1 \\ -1 & 0 \end{bmatrix}$$

$$BA = \begin{bmatrix} 0 & -1 \\ 1 & 0 \end{bmatrix}$$

$$AB \neq BA$$

$$(AB)^{-1} = B^{-1}A^{-1} = B \cdot A = BA = \begin{bmatrix} 0 & -1 \\ 1 & 0 \end{bmatrix}$$

---

### 考题2：秩的计算

**【题目】**求矩阵的秩
$$A = \begin{bmatrix} 1 & 2 & 0 & 3 \\ 2 & 4 & 1 & 6 \\ 3 & 6 & 1 & 9 \end{bmatrix}$$

**【解答】**

行变换：
$$\begin{bmatrix} 1 & 2 & 0 & 3 \\ 0 & 0 & 1 & 0 \\ 0 & 0 & 1 & 0 \end{bmatrix} \to \begin{bmatrix} 1 & 2 & 0 & 3 \\ 0 & 0 & 1 & 0 \\ 0 & 0 & 0 & 0 \end{bmatrix}$$

**答案**：$r(A) = 2$

---

### 考题3：逆矩阵计算

**【题目】**求
$$A = \begin{bmatrix} 1 & 0 & 0 \\ 2 & 1 & 0 \\ 3 & 2 & 1 \end{bmatrix}$$
的逆矩阵

**【快速解法】**（下三角矩阵）

$$A^{-1} = \begin{bmatrix} 1 & 0 & 0 \\ -2 & 1 & 0 \\ 1 & -2 & 1 \end{bmatrix}$$

**验证**：
$$AA^{-1} = \begin{bmatrix} 1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 1 \end{bmatrix}$$

---

## 五、快速判断技巧

### 🎯 秩的快速判断

1. **观察行列关系**
   - 有成比例的行 → 秩减少
   - 有零行 → 秩 < 行数

2. **利用定义**
   - $r(A) = k$ ⇔ 存在k阶非零子式，k+1阶全为0

3. **特殊矩阵**
   - 可逆矩阵：$r(A) = n$
   - 零矩阵：$r(O) = 0$

### ⚡ 逆矩阵快速求解

| 矩阵类型 | 逆矩阵 |
|---------|--------|
| 对角阵 | 对角元素取倒数 |
| 上/下三角 | 仍为三角，逐元素计算 |
| 正交阵 | $A^{-1} = A^T$ |
| 分块对角 | 各块分别求逆 |

---

## 六、练习题

1. 已知 $A^2 = A$，求 $(I-A)^{-1}$

2. 若 $AB = O$，$r(A) = n-1$，求 $r(B)$

3. 求矩阵 $\begin{bmatrix} 1 & 1 & 1 \\ 1 & 2 & 3 \\ 1 & 3 & t \end{bmatrix}$ 的秩与 $t$ 的关系

**提示**：
1) $I-A$  
2) $r(B) \leq 1$  
3) $t \neq 6$ 时秩=3，$t=6$ 时秩=2

---

**本章重点**：
- 矩阵乘法不可交换
- 秩：行阶梯形数非零行
- 逆矩阵：初等变换最常用

**下一章**：线性方程组
