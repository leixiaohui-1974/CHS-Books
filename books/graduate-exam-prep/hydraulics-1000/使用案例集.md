# 💼 《水力学1000题详解》使用案例集

## 📚 案例索引

本文档提供12个真实使用案例，展示如何将代码库应用于学习、考研和工程实践。

---

## 🎓 教学场景

### 案例1：课堂演示 - Bernoulli方程可视化

**场景**：教师在课堂上讲解Bernoulli方程

**使用代码**：`problem_126_bernoulli_pipe.py`

**操作步骤**：
```bash
# 1. 投影前准备
cd codes
python3 problem_126_bernoulli_pipe.py

# 2. 展示生成的图表
open problem_126_result.png
```

**教学效果**：
- ✅ 直观展示能量线和水力坡度线
- ✅ 清晰显示各点的压强、流速和位能
- ✅ 学生能够看到能量守恒的实际应用

**可扩展**：
```python
# 修改参数演示不同情况
from problem_126_bernoulli_pipe import BernoulliPipe

solver = BernoulliPipe()
solver.H1 = 20  # 修改上游水位
solver.d = 0.5  # 修改管径
solver.calculate()
solver.visualize()
```

---

### 案例2：实验课辅助 - 管道阻力实验

**场景**：学生进行管道阻力实验

**使用代码**：`problem_311_friction_factor.py`

**实验流程**：
1. 学生测量实际管道的流量、压降
2. 运行代码计算理论值
3. 对比实验值与理论值
4. 分析误差原因

**代码使用**：
```python
from problem_311_friction_factor import FrictionFactor

# 输入实验数据
solver = FrictionFactor()
solver.d = 0.05      # 实验管径
solver.v = 2.5       # 实验流速
solver.epsilon = 0.0001  # 实验管道粗糙度

# 计算理论值
solver.calculate_reynolds()
solver.calculate_friction_factor()

print(f"理论Re: {solver.Re:.0f}")
print(f"理论λ: {solver.lambda_f:.4f}")
print(f"理论压降: {solver.delta_p:.2f} Pa/m")

# 与实验值对比
experimental_lambda = 0.0235  # 实验测量值
error = abs(solver.lambda_f - experimental_lambda) / experimental_lambda * 100
print(f"误差: {error:.2f}%")
```

---

## 📝 考研复习

### 案例3：考研复习 - 管网平差专题

**场景**：考研学生攻克管网平差难点

**使用代码**：`problem_536_hardy_cross.py`

**学习计划**：

**Day 1: 理解原理**
```bash
# 运行代码，理解Hardy-Cross法原理
python3 problem_536_hardy_cross.py

# 重点观察：
# 1. 节点流量平衡
# 2. 回路水头平衡
# 3. 迭代收敛过程
```

**Day 2: 修改参数**
```python
from problem_536_hardy_cross import HardyCross

# 修改管网拓扑
network = HardyCross()
network.pipes[0]['L'] = 800  # 修改管长
network.pipes[0]['d'] = 0.35  # 修改管径

# 观察对收敛的影响
network.solve()
```

**Day 3: 模拟考题**
```
根据代码理解，手工计算一个简化管网
对比手算与代码结果
总结解题步骤和技巧
```

**学习成果**：
- ✅ 理解Hardy-Cross法的数学原理
- ✅ 掌握迭代计算的过程
- ✅ 能够快速求解管网问题

---

### 案例4：考前冲刺 - 综合题突破

**场景**：考前一周，攻克综合大题

**使用代码**：`problem_901-904`综合应用系列

**冲刺策略**：

**周一：水库系统**
```bash
python3 problem_901_comprehensive_reservoir.py
# 理解：堰流→隧洞→水跃→明渠的完整流程
```

**周二：泵站系统**
```bash
python3 problem_902_pump_station_system.py
# 理解：管路→泵选型→调节→经济分析
```

**周三：渠道系统**
```bash
python3 problem_903_canal_bridge_system.py
# 理解：Manning→收缩→壅水→水面线
```

**周四：水电站**
```bash
python3 problem_904_integrated_water_project.py
# 理解：全系统整合分析
```

**周五-周六：模拟练习**
```
选择历年真题中的综合题
参考代码的解题思路
限时独立完成
对比答案，查漏补缺
```

---

## 🏗️ 工程应用

### 案例5：城市供水管网设计

**场景**：某城市新区供水管网初步设计

**项目参数**：
- 服务人口：50,000人
- 用水定额：200 L/人·天
- 管网总长：15 km
- 地形高差：20 m

**使用代码序列**：

**步骤1：流量计算**
```python
# 计算设计流量
population = 50000
water_quota = 200  # L/人·天
K_daily = 1.3      # 日变化系数
K_hourly = 1.5     # 时变化系数

Q_avg = population * water_quota / 1000 / 86400  # m³/s
Q_design = Q_avg * K_daily * K_hourly
print(f"设计流量: {Q_design:.3f} m³/s")
```

**步骤2：管道计算**（使用`problem_351_pipe_calculation.py`）
```python
from problem_351_pipe_calculation import PipeCalculation

# 主干管计算
main_pipe = PipeCalculation()
main_pipe.Q = Q_design
main_pipe.L = 3000  # 主干管长度
main_pipe.d = 0.6   # 初选管径

main_pipe.calculate_all()
print(f"主干管水头损失: {main_pipe.h_f:.2f} m")
print(f"流速: {main_pipe.v:.2f} m/s")

# 根据规范检查流速（0.6-3.0 m/s）
if 0.6 <= main_pipe.v <= 3.0:
    print("✓ 流速符合规范")
else:
    print("✗ 需要调整管径")
```

**步骤3：管网平差**（使用`problem_536_hardy_cross.py`）
```python
# 建立管网模型
# 根据实际拓扑结构修改代码中的管网数据
# 运行迭代计算
# 获得各管段流量和节点水压
```

**步骤4：水泵选型**（使用`problem_791_pump_operation.py`）
```python
from problem_791_pump_operation import PumpOperation

pump_system = PumpOperation()
# 修改为实际管路特性
# 计算所需扬程和流量
# 选择合适的水泵型号
```

**设计成果**：
- ✅ 管网布置方案
- ✅ 管径选择依据
- ✅ 水泵选型报告
- ✅ 水头损失计算书

---

### 案例6：水库泄洪能力校核

**场景**：某水库需要校核泄洪能力

**水库参数**：
- 设计洪水位：250 m
- 泄洪隧洞：直径5m，长度800m
- 溢洪道：宽30m，堰顶高程245m

**使用代码**：`problem_901_comprehensive_reservoir.py`

**校核流程**：

**步骤1：修改代码参数**
```python
from problem_901_comprehensive_reservoir import ReservoirSystem

system = ReservoirSystem()

# 修改为实际参数
system.H_reservoir = 250  # 水库水位
system.H_weir = 245       # 堰顶高程
system.b_weir = 30        # 堰宽
system.d_tunnel = 5       # 隧洞直径
system.L_tunnel = 800     # 隧洞长度

# 重新计算
system.calculate_all()
```

**步骤2：分析结果**
```python
Q_total = system.Q_weir + system.Q_tunnel
print(f"总泄流能力: {Q_total:.2f} m³/s")

# 与设计洪峰流量对比
Q_design_flood = 5000  # m³/s
if Q_total >= Q_design_flood:
    print("✓ 泄洪能力满足要求")
else:
    deficit = Q_design_flood - Q_total
    print(f"✗ 泄洪能力不足 {deficit:.2f} m³/s")
    print("建议措施：")
    print("1. 加宽溢洪道")
    print("2. 增加泄洪孔")
    print("3. 降低堰顶高程")
```

**步骤3：生成报告**
```python
system.print_results()
system.visualize()

# 报告内容：
# - 各泄洪设施分流比
# - 下游水跃特性
# - 消能工设计建议
# - 优化方案对比
```

---

### 案例7：农田灌溉渠道设计

**场景**：设计农田灌溉渠道系统

**项目参数**：
- 灌溉面积：500 ha
- 设计流量：2.5 m³/s
- 地形坡度：1/2000
- 渠道形式：梯形断面

**使用代码**：`problem_456_manning_formula.py`

**设计步骤**：

**步骤1：断面设计**
```python
from problem_456_manning_formula import ManningFormula

channel = ManningFormula()

# 设计参数
channel.Q = 2.5       # 设计流量
channel.i = 0.0005    # 渠道坡度
channel.n = 0.025     # 土渠糙率
channel.m = 1.5       # 边坡系数

# 尝试不同底宽
for b in [2.0, 2.5, 3.0, 3.5]:
    channel.b = b
    channel.calculate_trapezoidal_channel()
    
    print(f"\n底宽 b = {b} m:")
    print(f"  水深: h = {channel.h:.3f} m")
    print(f"  流速: v = {channel.v:.3f} m/s")
    print(f"  Fr数: Fr = {channel.Fr:.3f}")
    
    # 检查规范要求
    if 0.4 <= channel.v <= 1.5 and channel.h <= 2.0:
        print(f"  ✓ 满足要求")
        break
```

**步骤2：水面线分析**（使用`problem_471_gradually_varied.py`）
```python
# 分析渠道中的水面线类型
# 确定控制断面位置
# 计算沿程水深变化
```

**步骤3：渡槽设计**（使用`problem_903_canal_bridge_system.py`）
```python
# 如果需要跨越道路
# 设计渡槽断面和结构
# 分析壅水影响
```

**设计成果**：
- ✅ 渠道断面图
- ✅ 纵断面设计
- ✅ 水面线计算
- ✅ 建筑物设计

---

## 🔬 科研应用

### 案例8：地下水资源评价

**场景**：评价某地区地下水可开采量

**研究区域**：
- 面积：100 km²
- 含水层类型：承压含水层
- 已有观测井：3口

**使用代码序列**：

**步骤1：单井抽水试验分析**（使用`problem_696_well_flow.py`）
```python
from problem_696_well_flow import WellFlow

# 第1口井抽水试验
well1 = WellFlow()
well1.Q_confined = 2000  # m³/d
well1.r0 = 0.2           # 井半径
well1.M = 50             # 含水层厚度
well1.H0 = 80            # 初始水头

well1.calculate_confined_well()
print(f"第1口井影响半径: {well1.R_confined:.0f} m")
```

**步骤2：参数反算**（使用`problem_701_groundwater_level.py`）
```python
from problem_701_groundwater_level import GroundwaterLevel

# 利用观测井数据反算水文地质参数
params = GroundwaterLevel()
params.Q = 2000          # m³/d
params.M = 50            # m
params.r1 = 50           # 观测井1距离
params.s1 = 2.5          # 观测井1降深
params.r2 = 100          # 观测井2距离
params.s2 = 1.2          # 观测井2降深

params.calculate_parameters()
print(f"渗透系数: k = {params.k:.2f} m/d")
```

**步骤3：可开采量估算**
```python
# 根据参数计算区域可开采量
area = 100e6  # m²
recharge_rate = 300  # mm/yr

Q_recharge = area * (recharge_rate / 1000) / 365  # m³/d
print(f"年补给量: {Q_recharge:.0f} m³/d")

# 计算允许开采量（补给量的80%）
Q_allowable = Q_recharge * 0.8
print(f"允许开采量: {Q_allowable:.0f} m³/d")

# 计算需要的井数
Q_per_well = 2000  # m³/d/井
n_wells = int(Q_allowable / Q_per_well)
print(f"建议井数: {n_wells} 口")
```

**研究成果**：
- ✅ 水文地质参数
- ✅ 可开采量评估
- ✅ 井位布局建议
- ✅ 开采方案优化

---

### 案例9：水击防护方案优化

**场景**：某长距离输水管道水击防护

**系统参数**：
- 管道长度：10 km
- 管径：1.2 m
- 流量：1.5 m³/s
- 材质：钢管

**使用代码**：`problem_561_water_hammer.py`

**研究内容**：

**方案1：延长关闭时间**
```python
from problem_561_water_hammer import WaterHammer

baseline = WaterHammer()
baseline.L = 10000    # 管长
baseline.d = 1.2      # 管径
baseline.Q = 1.5      # 流量
baseline.t_close = 10  # 关闭时间

baseline.calculate_all()
print(f"方案1 - 关闭时间{baseline.t_close}s:")
print(f"  最大压力升: {baseline.delta_H:.2f} m")
print(f"  最大压力: {baseline.H_max:.2f} m")
```

**方案2：设置调压井**
```python
# 修改参数模拟调压井效果
with_surge_tank = WaterHammer()
with_surge_tank.L = 5000  # 调压井分段
# ... 其他参数
with_surge_tank.calculate_all()
```

**方案3：设置空气阀**
```python
# 模拟空气阀缓冲效果
# 修改波速和衰减系数
```

**优化结果**：
```python
schemes = {
    '延长关闭时间': 350,    # 压力升(m)
    '设置调压井': 180,
    '设置空气阀': 220,
    '综合方案': 120
}

# 选择最优方案
best_scheme = min(schemes, key=schemes.get)
print(f"推荐方案: {best_scheme}")
print(f"压力升: {schemes[best_scheme]} m")
```

---

## 💼 咨询服务

### 案例10：泵站改造技术咨询

**场景**：老旧泵站节能改造咨询

**现状**：
- 原有2台水泵，运行20年
- 效率下降至60%
- 年电费200万元

**使用代码**：`problem_796_pump_parallel.py` + `problem_791_pump_operation.py`

**咨询流程**：

**步骤1：现状分析**
```python
from problem_791_pump_operation import PumpOperation

# 模拟现有泵站
old_system = PumpOperation()
old_system.pump.eta0 = 0.60  # 老化效率
old_system.calculate()

print(f"现状年耗电: {old_system.annual_cost / 0.6:.0f} kWh")
```

**步骤2：改造方案设计**
```python
# 方案A：更换高效泵
new_pump = PumpOperation()
new_pump.pump.eta0 = 0.85
new_pump.calculate()

# 方案B：变频调速
vfd_system = PumpOperation()
# 模拟变频效果

# 方案C：综合改造
comprehensive = PumpOperation()
# ...
```

**步骤3：经济分析**
```python
# 计算投资回收期
schemes = {
    '方案A': {'投资': 150, '年节约': 80},   # 万元
    '方案B': {'投资': 80, '年节约': 50},
    '方案C': {'投资': 200, '年节约': 100}
}

for name, data in schemes.items():
    payback = data['投资'] / data['年节约']
    print(f"{name} 投资回收期: {payback:.1f} 年")
```

**咨询报告**：
- ✅ 现状诊断
- ✅ 改造方案
- ✅ 经济对比
- ✅ 实施建议

---

### 案例11：水资源论证

**场景**：某工业项目取水许可论证

**项目需求**：
- 取水量：5000 m³/d
- 取水方式：地下水井
- 取水层：潜水含水层

**使用代码序列**：

**第1部分：取水可行性**（使用`problem_696_well_flow.py`）
```python
# 计算单井出水能力
# 确定需要的井数
# 分析对周边的影响
```

**第2部分：环境影响**（使用`problem_701_groundwater_level.py`）
```python
# 计算降落漏斗范围
# 分析对周边取水户的影响
# 评估生态环境影响
```

**第3部分：论证报告**
```
1. 取水水源可靠性分析
2. 取水对周边影响分析
3. 退水方案可行性论证
4. 节水措施建议
5. 监测方案设计
```

---

## 🎯 特殊应用

### 案例12：软件二次开发 - Web应用

**场景**：开发在线水力计算工具

**技术栈**：Python + Flask + 代码库

**开发思路**：

**后端代码**（`app.py`）：
```python
from flask import Flask, request, jsonify
from problem_351_pipe_calculation import PipeCalculation

app = Flask(__name__)

@app.route('/api/pipe_calc', methods=['POST'])
def pipe_calculation():
    data = request.json
    
    solver = PipeCalculation()
    solver.Q = float(data['flow'])
    solver.d = float(data['diameter'])
    solver.L = float(data['length'])
    
    solver.calculate_all()
    
    return jsonify({
        'velocity': solver.v,
        'reynolds': solver.Re,
        'friction_factor': solver.lambda_f,
        'head_loss': solver.h_f
    })

if __name__ == '__main__':
    app.run(debug=True)
```

**前端页面**（`index.html`）：
```html
<form id="pipeForm">
    <input name="flow" placeholder="流量(m³/s)">
    <input name="diameter" placeholder="管径(m)">
    <input name="length" placeholder="长度(m)">
    <button type="submit">计算</button>
</form>
<div id="results"></div>

<script>
document.getElementById('pipeForm').addEventListener('submit', async (e) => {
    e.preventDefault();
    const formData = new FormData(e.target);
    const data = Object.fromEntries(formData);
    
    const response = await fetch('/api/pipe_calc', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify(data)
    });
    
    const results = await response.json();
    document.getElementById('results').innerHTML = 
        `流速: ${results.velocity.toFixed(2)} m/s<br>
         雷诺数: ${results.reynolds.toFixed(0)}<br>
         阻力系数: ${results.friction_factor.toFixed(4)}<br>
         水头损失: ${results.head_loss.toFixed(2)} m`;
});
</script>
```

**功能扩展**：
- 图表可视化（使用Plotly.js）
- 用户账号系统
- 计算历史记录
- 批量计算功能
- PDF报告生成

---

## 📋 案例总结

### 按场景分类

| 场景类别 | 适用案例 | 关键代码 |
|---------|---------|---------|
| 教学演示 | 案例1-2 | 126, 311 |
| 考研复习 | 案例3-4 | 536, 901-904 |
| 工程设计 | 案例5-7 | 351, 536, 791, 901, 903 |
| 科研分析 | 案例8-9 | 561, 696, 701 |
| 咨询服务 | 案例10-11 | 791, 796, 696, 701 |
| 软件开发 | 案例12 | 全部可集成 |

### 使用技巧总结

1. **参数修改**：直接在`__init__`方法中修改
2. **批量计算**：用循环调用不同参数
3. **结果导出**：添加JSON/CSV导出功能
4. **可视化定制**：修改`visualize`方法
5. **集成应用**：导入类直接使用

---

*案例集更新时间：2025-11-10*
*版本：v1.0*
*欢迎补充更多实际案例*
