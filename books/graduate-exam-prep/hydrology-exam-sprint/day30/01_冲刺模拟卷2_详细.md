# Day 30：冲刺模拟卷2

**适用对象**：考前1天  
**考试时间**：180分钟  
**总分**：150分

---

## 一、选择题（每题3分，共30分）

### 1. 年径流的年内分配

**题目**：我国北方河流年径流主要集中在（  ）

A. 春季  
B. 夏季  
C. 秋季  
D. 冬季

**答案**：B

**解析**：北方河流径流主要由夏季降雨补给，汛期集中在6-9月。

---

### 2. 设计洪水成果合理性检查

**题目**：设计洪水成果合理性检查的主要方法不包括（  ）

A. 地区综合法  
B. 历史洪水调查  
C. 暴雨资料分析  
D. 水库调洪计算

**答案**：D

**解析**：合理性检查方法包括地区综合、历史洪水、暴雨资料等，水库调洪是应用设计洪水的方法。

---

### 3. 流量资料插补延长

**题目**：对短系列水文资料进行插补延长，最常用的方法是（  ）

A. 水文比拟法  
B. 相关分析法  
C. 降水径流关系法  
D. 水量平衡法

**答案**：B

**解析**：相关分析法（建立参证站与设计站的相关关系）是最常用的插补延长方法。

---

### 4. 设计年径流

**题目**：某站多年平均径流量$\overline{W}=100$亿m³，$C_v=0.25$，$C_s=2C_v$，P=75%的年径流量约为（  ）亿m³（查表$K_p=0.75$）

A. 65  
B. 75  
C. 85  
D. 95

**答案**：B

**计算**：$W_{75\%} = K_p \cdot \overline{W} = 0.75 \times 100 = 75$ 亿m³

---

### 5. 水文预报

**题目**：下列关于水文预报的叙述，正确的是（  ）

A. 短期预报预见期为1-3天  
B. 中期预报预见期为10-30天  
C. 长期预报预见期为1-3个月  
D. 以上都对

**答案**：D

**解析**：短期1-3天，中期10-30天，长期1-3月，超长期>3月。

---

### 6-10题（略）

---

## 二、填空题（每空2分，共20分）

### 1. 水文统计

对水文系列进行代表性分析，主要检验其____、____和____。

**答案**：可靠性；一致性；代表性（或：独立性、一致性、代表性）

---

### 2. 单位线

单位线的三要素包括：单位净雨、单位____和____。

**答案**：历时；流域

---

### 3. 洪水预报方案

常用的洪水预报方案有：水位流量关系法、____、____和河道洪水演算法。

**答案**：单位线法；降雨径流相关法

---

### 4-5题（略）

---

## 三、计算题（共100分）

### 题1：年径流频率分析（25分）

**题目**：
某站40年年径流量系列经统计：均值$\overline{W}=50$亿m³，均方差$\sigma=15$亿m³。

**要求**：
(1) 计算变差系数$C_v$和偏态系数$C_s$（假设$C_s=2C_v$）（5分）  
(2) 查P-III分布表，求P=50%、75%、90%、95%的年径流量（10分）  
(3) 绘制理论频率曲线（5分）  
(4) 计算P=90%的年供水保证率下的可供水量（5分）

---

**解答**：

**(1) 统计参数**

**变差系数**：
$$
C_v = \frac{\sigma}{\overline{W}} = \frac{15}{50} = 0.30
$$

**偏态系数**：
$$
C_s = 2C_v = 2 \times 0.30 = 0.60
$$

**(2) 不同频率年径流量**

查P-III分布表（$C_v=0.30$，$C_s/C_v=2$）：

| 频率P | 模比系数$K_p$ | 年径流量$W_p$（亿m³） |
|-------|--------------|---------------------|
| 50% | 1.00 | $1.00 \times 50 = 50$ |
| 75% | 0.78 | $0.78 \times 50 = 39$ |
| 90% | 0.60 | $0.60 \times 50 = 30$ |
| 95% | 0.52 | $0.52 \times 50 = 26$ |

**(3) 理论频率曲线**

以P为横轴（概率格纸），$W_p$为纵轴，绘制曲线通过上述4点。

**图略**（P-W曲线，非线性下凹）

**(4) 可供水量**

P=90%保证率下，年径流量为30亿m³。

考虑水库调节，设调节系数α=1.5（年调节水库），则：
$$
Q_{可供} = \alpha \times W_{90\%} = 1.5 \times 30 = 45 \text{ 亿m³/年}
$$

或按多年调节：
$$
Q_{可供} \approx W_{90\%} + \beta(W_{50\%} - W_{90\%}) = 30 + 0.5(50 - 30) = 40 \text{ 亿m³/年}
$$

（具体值需根据水库特性确定）

---

### 题2：相关分析法插补延长（25分）

**题目**：
设计站（A站）实测15年年径流量，参证站（B站）实测50年年径流量。同步期15年数据如下：

| 年份 | 1-5 | 6-10 | 11-15 |
|------|-----|------|-------|
| A站(亿m³) | 25, 30, 22, 28, 24 | 32, 26, 29, 23, 27 | 31, 25, 28, 26, 30 |
| B站(亿m³) | 50, 60, 44, 56, 48 | 64, 52, 58, 46, 54 | 62, 50, 56, 52, 60 |

参证站B站前35年均值$\overline{W_B}=52$亿m³。

**要求**：
(1) 建立A、B站线性相关方程（15分）  
(2) 计算相关系数r，检验显著性（5分）  
(3) 估算A站40年（采用B站40年均值）的多年平均径流量（5分）

---

**解答**：

**(1) 相关方程**

设 $W_A = a + b W_B$

同步期数据：
- $\overline{W_A} = \frac{25+30+...+30}{15} = 27.0$ 亿m³
- $\overline{W_B} = \frac{50+60+...+60}{15} = 54.0$ 亿m³

**回归系数**：
$$
b = \frac{\sum (W_{A,i} - \overline{W_A})(W_{B,i} - \overline{W_B})}{\sum (W_{B,i} - \overline{W_B})^2}
$$

计算：
$$
\sum (W_A - \overline{W_A})(W_B - \overline{W_B}) = (25-27)(50-54) + (30-27)(60-54) + \ldots \approx 420
$$

$$
\sum (W_B - \overline{W_B})^2 = (50-54)^2 + (60-54)^2 + \ldots \approx 840
$$

$$
b = \frac{420}{840} = 0.5
$$

**截距**：
$$
a = \overline{W_A} - b \overline{W_B} = 27 - 0.5 \times 54 = 0
$$

**相关方程**：
$$
W_A = 0.5 W_B
$$

**(2) 相关系数**

$$
r = \frac{\sum (W_A - \overline{W_A})(W_B - \overline{W_B})}{\sqrt{\sum (W_A - \overline{W_A})^2 \sum (W_B - \overline{W_B})^2}}
$$

$$
\sum (W_A - \overline{W_A})^2 \approx 210
$$

$$
r = \frac{420}{\sqrt{210 \times 840}} = \frac{420}{\sqrt{176400}} = \frac{420}{420} = 1.0
$$

（实际数据相关系数约0.95-0.98，此处简化为1.0）

**显著性检验**：
$$
t = \frac{r\sqrt{n-2}}{\sqrt{1-r^2}} = \frac{1.0 \times \sqrt{13}}{0} \to \infty
$$

通过显著性检验（$t > t_{0.05,13}=2.16$）

**(3) 多年平均径流量**

B站40年均值约为$\overline{W_B,40} = 52$亿m³（前35年）

A站40年均值估算：
$$
\overline{W_A,40} = 0.5 \times 52 = 26 \text{ 亿m³}
$$

---

### 题3：水库兴利调节计算（30分）

**题目**：
某水库兴利库容10亿m³，死库容5亿m³，年供水量30亿m³。某典型枯水年12个月入库径流为：

| 月份 | 1-6 | 7-12 |
|------|-----|------|
| W_in(亿m³) | 1.5, 1.2, 1.0, 1.8, 3.5, 4.5 | 6.0, 5.5, 4.0, 2.5, 1.8, 1.5 |

**要求**：
(1) 用时历法计算各月末水位（假设库容-水位关系线性：$Z = 100 + V$）（20分）  
(2) 判断能否满足供水要求（5分）  
(3) 提出改进措施（5分）

---

**解答**：

**(1) 逐月调节计算**

月供水量：$Q_{供} = 30 / 12 = 2.5$ 亿m³/月

| 月份 | $W_{入}$ | $W_{供}$ | $W_{入}-W_{供}$ | $V_{末}$ | $Z_{末}$ | 备注 |
|------|---------|---------|----------------|---------|---------|------|
| 初始 | - | - | - | 15 | 115 | 年初满库 |
| 1 | 1.5 | 2.5 | -1.0 | 14.0 | 114 | |
| 2 | 1.2 | 2.5 | -1.3 | 12.7 | 112.7 | |
| 3 | 1.0 | 2.5 | -1.5 | 11.2 | 111.2 | |
| 4 | 1.8 | 2.5 | -0.7 | 10.5 | 110.5 | |
| 5 | 3.5 | 2.5 | +1.0 | 11.5 | 111.5 | |
| 6 | 4.5 | 2.5 | +2.0 | 13.5 | 113.5 | |
| 7 | 6.0 | 2.5 | +3.5 | 15.0 | 115 | **满库弃水** |
| 8 | 5.5 | 2.5 | +3.0 | 15.0 | 115 | 弃水3.0 |
| 9 | 4.0 | 2.5 | +1.5 | 15.0 | 115 | 弃水1.5 |
| 10 | 2.5 | 2.5 | 0 | 15.0 | 115 | |
| 11 | 1.8 | 2.5 | -0.7 | 14.3 | 114.3 | |
| 12 | 1.5 | 2.5 | -1.0 | 13.3 | 113.3 | |

**(2) 供水满足情况**

- 最低库容：10.5亿m³（4月末） > 死库容5亿m³ ✓
- 全年无断供情况
- **满足供水要求**

但弃水量 = 3.0 + 1.5 = 4.5亿m³，利用率不高。

**(3) 改进措施**

1. **增加库容**：提高兴利库容至12-15亿m³，减少弃水
2. **联合调度**：与上游其他水库联合调度，丰水期多蓄
3. **需求侧管理**：汛期增加供水量至3.5亿m³/月，非汛期减至2.0亿m³/月
4. **生态补水**：弃水用于下游生态用水

---

### 题4：综合分析题（20分）

**题目**：
某流域拟建水库，集水面积500 km²，多年平均降水量1000 mm，蒸发量600 mm。水库正常蓄水位时水面面积10 km²。

**要求**：
(1) 估算流域多年平均径流量（不考虑水库蒸发）（5分）  
(2) 考虑水库蒸发后，实际可利用水量（5分）  
(3) 若设计保证率P=90%的年径流量为平均的0.65倍，水库可供水量为年径流的70%，计算设计供水量（5分）  
(4) 分析该地区水资源开发利用存在的问题（5分）

---

**解答**：

**(1) 多年平均径流量**

径流深：
$$
R = P - E = 1000 - 600 = 400 \text{ mm}
$$

径流量：
$$
W = R \times F / 1000 = 400 \times 500 / 1000 = 200 \text{ 万m³} = 2.0 \text{ 亿m³}
$$

**(2) 考虑水库蒸发**

水库额外蒸发量（相对陆面）：
$$
\Delta E = (E_{水面} - E_{陆面}) \times A_{水面}
$$

假设水面蒸发量为陆面的1.5倍：
$$
E_{水面} = 1.5 \times 600 = 900 \text{ mm}
$$

$$
\Delta E = (900 - 600) \times 10 / 1000 = 3 \text{ 万m³} = 0.03 \text{ 亿m³}
$$

实际可利用：
$$
W_{可利用} = W - \Delta E = 2.0 - 0.03 = 1.97 \text{ 亿m³}
$$

**(3) 设计供水量**

P=90%年径流量：
$$
W_{90\%} = 0.65 \times 2.0 = 1.30 \text{ 亿m³}
$$

设计供水量：
$$
Q_{供} = 0.70 \times 1.30 = 0.91 \text{ 亿m³/年}
$$

**(4) 问题分析**

1. **水资源量偏少**：人均/亩均水量低
2. **年际变化大**：$C_v$较大（从0.65倍推断），需要较大调节库容
3. **蒸发损失**：水面蒸发虽占比小(1.5%)，但干旱区可能更严重
4. **开发利用率**：70%的利用率较合理，但需平衡生态用水
5. **水质保护**：水库建成后需防止水体富营养化

**建议**：
- 节水灌溉
- 水资源综合规划
- 加强流域水土保持
- 生态补偿机制

---

## 四、Python综合实现

```python
import numpy as np
import matplotlib.pyplot as plt
from scipy import stats
from scipy.optimize import curve_fit


class HydrologyComprehensive:
    """水文分析综合工具"""
    
    @staticmethod
    def pearson_iii_parameters(data):
        """
        计算P-III分布参数
        
        参数:
            data: 水文系列数组
        
        返回:
            mean, Cv, Cs: 均值、变差系数、偏态系数
        """
        mean = np.mean(data)
        std = np.std(data, ddof=1)
        Cv = std / mean
        
        # 偏态系数
        n = len(data)
        Cs = n * np.sum(((data - mean) / std)**3) / ((n - 1) * (n - 2))
        
        return mean, Cv, Cs
    
    @staticmethod
    def pearson_iii_quantile(mean, Cv, Cs, p):
        """
        P-III分布分位数计算（查表法简化）
        
        参数:
            mean: 均值
            Cv: 变差系数
            Cs: 偏态系数
            p: 频率（0-1）
        
        返回:
            quantile: 分位数
        """
        # 简化：使用查表Kp值（实际需插值）
        # 这里使用近似公式
        Cs_Cv = Cs / Cv if Cv > 0 else 2
        
        # 正态分布分位数
        z = stats.norm.ppf(1 - p)
        
        # P-III修正（Wilson-Hilferty变换）
        if Cs > 0:
            k = 2 / Cs
            Kp = 1 + Cv * (z - Cs/6 + (z**2 - 1) * Cs/36)
        else:
            Kp = 1 + Cv * z
        
        quantile = mean * Kp
        
        return quantile
    
    @staticmethod
    def correlation_extension(x, y):
        """
        相关分析插补延长
        
        参数:
            x, y: 同步期两站数据
        
        返回:
            slope, intercept, r: 回归参数和相关系数
        """
        slope, intercept, r, p_value, std_err = stats.linregress(x, y)
        
        return slope, intercept, r
    
    @staticmethod
    def reservoir_regulation(inflow, demand, V_init, V_active, V_dead):
        """
        水库调节计算
        
        参数:
            inflow: 入库流量序列
            demand: 需水量序列
            V_init: 初始库容
            V_active: 兴利库容
            V_dead: 死库容
        
        返回:
            storage: 库容过程
            release: 出库流量
            deficit: 缺水量
        """
        n = len(inflow)
        storage = np.zeros(n + 1)
        release = np.zeros(n)
        deficit = np.zeros(n)
        
        storage[0] = V_init
        V_max = V_active + V_dead
        
        for i in range(n):
            # 水量平衡
            V_next = storage[i] + inflow[i] - demand[i]
            
            # 约束
            if V_next > V_max:
                # 弃水
                release[i] = inflow[i] - (V_max - storage[i])
                storage[i+1] = V_max
            elif V_next < V_dead:
                # 缺水
                release[i] = inflow[i] + storage[i] - V_dead
                deficit[i] = demand[i] - release[i]
                storage[i+1] = V_dead
            else:
                # 正常
                release[i] = demand[i]
                storage[i+1] = V_next
        
        return storage, release, deficit


# 综合演示
print("="*60)
print("水文分析综合演示")
print("="*60)

hc = HydrologyComprehensive()

# 1. P-III分布分析
print("\n1. 年径流频率分析（P-III分布）")

# 生成40年模拟数据
np.random.seed(42)
annual_runoff = np.random.gamma(shape=16, scale=3.125, size=40)  # 均值50

mean, Cv, Cs = hc.pearson_iii_parameters(annual_runoff)

print(f"\n统计参数：")
print(f"  均值: {mean:.2f} 亿m³")
print(f"  Cv: {Cv:.3f}")
print(f"  Cs: {Cs:.3f}")
print(f"  Cs/Cv: {Cs/Cv:.2f}")

# 不同频率设计值
frequencies = [0.10, 0.25, 0.50, 0.75, 0.90, 0.95]
print(f"\n设计值：")
print(f"{'频率P':<10s} {'年径流(亿m³)':<15s}")
print("-" * 25)
for p in frequencies:
    Wp = hc.pearson_iii_quantile(mean, Cv, Cs, p)
    print(f"{p*100:>6.0f}%     {Wp:>10.2f}")

# 2. 相关分析
print("\n" + "="*60)
print("2. 相关分析插补延长")

# 模拟A、B两站数据
B_station = np.random.normal(54, 6, 15)
A_station = 0.5 * B_station + np.random.normal(0, 1, 15)

slope, intercept, r = hc.correlation_extension(B_station, A_station)

print(f"\n回归方程: A = {intercept:.2f} + {slope:.3f} × B")
print(f"相关系数: r = {r:.3f}")
print(f"决定系数: R² = {r**2:.3f}")

# t检验
n = len(B_station)
t_stat = r * np.sqrt(n - 2) / np.sqrt(1 - r**2)
t_crit = stats.t.ppf(0.975, n - 2)

print(f"\n显著性检验:")
print(f"  t统计量: {t_stat:.2f}")
print(f"  临界值t(0.05,{n-2}): {t_crit:.2f}")
print(f"  结论: {'通过' if abs(t_stat) > t_crit else '不通过'}显著性检验")

# 3. 水库调节
print("\n" + "="*60)
print("3. 水库兴利调节计算")

monthly_inflow = np.array([1.5, 1.2, 1.0, 1.8, 3.5, 4.5,
                          6.0, 5.5, 4.0, 2.5, 1.8, 1.5])
monthly_demand = np.full(12, 2.5)

storage, release, deficit = hc.reservoir_regulation(
    monthly_inflow, monthly_demand,
    V_init=15, V_active=10, V_dead=5
)

print(f"\n调节结果：")
print(f"  最低库容: {storage[1:].min():.2f} 亿m³")
print(f"  最高库容: {storage.max():.2f} 亿m³")
print(f"  总缺水量: {deficit.sum():.2f} 亿m³")
print(f"  供水保证率: {(1 - np.sum(deficit > 0) / 12) * 100:.1f}%")

# 可视化
fig, axes = plt.subplots(2, 2, figsize=(14, 10))

# P-III频率曲线
ax = axes[0, 0]
sorted_data = np.sort(annual_runoff)[::-1]
emp_freq = np.arange(1, len(sorted_data) + 1) / (len(sorted_data) + 1)

ax.plot(emp_freq * 100, sorted_data, 'bo', markersize=6, label='经验点据')

# 理论曲线
p_range = np.linspace(0.01, 0.99, 100)
W_theory = [hc.pearson_iii_quantile(mean, Cv, Cs, p) for p in p_range]
ax.plot(p_range * 100, W_theory, 'r-', linewidth=2, label='理论曲线')

ax.set_xlabel('频率 P (%)', fontsize=11)
ax.set_ylabel('年径流量 (亿m³)', fontsize=11)
ax.set_title('P-III分布频率曲线', fontsize=12, fontweight='bold')
ax.legend()
ax.grid(True, alpha=0.3)
ax.invert_xaxis()

# 相关分析
ax = axes[0, 1]
ax.scatter(B_station, A_station, s=80, alpha=0.6, edgecolors='black')

# 回归线
B_range = np.linspace(B_station.min(), B_station.max(), 100)
A_pred = intercept + slope * B_range
ax.plot(B_range, A_pred, 'r-', linewidth=2,
       label=f'A = {intercept:.1f} + {slope:.2f}B\nr = {r:.3f}')

ax.set_xlabel('B站年径流 (亿m³)', fontsize=11)
ax.set_ylabel('A站年径流 (亿m³)', fontsize=11)
ax.set_title('相关分析与回归', fontsize=12, fontweight='bold')
ax.legend()
ax.grid(True, alpha=0.3)

# 水库调节过程
ax = axes[1, 0]
months = np.arange(1, 13)

ax.bar(months - 0.3, monthly_inflow, width=0.3, label='入库', color='skyblue')
ax.bar(months, monthly_demand, width=0.3, label='需水', color='lightcoral')
ax.bar(months + 0.3, release, width=0.3, label='供水', color='lightgreen')

ax.set_xlabel('月份', fontsize=11)
ax.set_ylabel('水量 (亿m³)', fontsize=11)
ax.set_title('水库逐月调节', fontsize=12, fontweight='bold')
ax.legend()
ax.grid(True, alpha=0.3, axis='y')

# 库容过程
ax = axes[1, 1]
ax.plot(np.arange(13), storage, 'go-', linewidth=2, markersize=8)
ax.axhline(15, color='blue', linestyle='--', label='总库容', linewidth=1.5)
ax.axhline(5, color='red', linestyle='--', label='死库容', linewidth=1.5)
ax.fill_between(np.arange(13), 5, storage, alpha=0.3, color='green')

ax.set_xlabel('月末', fontsize=11)
ax.set_ylabel('库容 (亿m³)', fontsize=11)
ax.set_title('库容变化过程', fontsize=12, fontweight='bold')
ax.legend()
ax.grid(True, alpha=0.3)

plt.tight_layout()
plt.savefig('mock_exam2_comprehensive.png', dpi=300, bbox_inches='tight')
plt.show()

print("\n" + "="*60)
print("综合演示完成！图表已保存。")
print("="*60)
```

---

## 五、考前提示

### 1. 时间分配策略

| 题型 | 分值 | 建议时间 | 策略 |
|------|------|---------|------|
| 选择题 | 30分 | 20分钟 | 快速判断，不纠结 |
| 填空题 | 20分 | 15分钟 | 关键词准确 |
| 计算题 | 100分 | 120分钟 | 步骤完整，单位正确 |
| 检查 | - | 25分钟 | 重点检查计算题 |

### 2. 常见失分点

1. **单位错误**：m³/s与m³混淆，mm与m混淆
2. **公式记混**：Manning公式、推理公式系数
3. **查表错误**：P-III分布表Cv、Cs/Cv搞反
4. **概念混淆**：频率P与重现期T、保证率与保证程度
5. **过程不全**：直接写答案，无中间步骤

### 3. 快速提分技巧

**选择题**：
- 排除法（先排除明显错误选项）
- 量级检验（数量级明显不对的排除）
- 单位检验（单位不匹配的排除）

**计算题**：
- **先写公式**：即使不会算，写对公式有步骤分
- **标注单位**：每个物理量都带单位
- **合理性检验**：结果要在合理范围（如径流系数0-1）
- **关键数字圈出**：方便检查时核对

### 4. 考场注意事项

1. **先浏览全卷**：了解题型分布，心中有数
2. **先易后难**：确保基础题不丢分
3. **合理取舍**：难题实在不会，及时放弃，确保其他题完成
4. **字迹清晰**：计算过程工整，方便阅卷老师找步骤分
5. **保持冷静**：遇到难题深呼吸，调整心态

---

## 本卷要点

1. **年径流分析**：P-III分布、频率曲线、设计径流
2. **资料插补**：相关分析法、回归方程、显著性检验
3. **水库调节**：兴利调节、逐月平衡、供水保证率
4. **综合应用**：水资源评价、问题分析、对策建议

---

**最后祝愿**：
✨ 沉着冷静，发挥水平  
✨ 思路清晰，步骤完整  
✨ 取得理想成绩，金榜题名！

---

**30天冲刺完成！你已经准备好了！相信自己，加油！🎓🎉**
