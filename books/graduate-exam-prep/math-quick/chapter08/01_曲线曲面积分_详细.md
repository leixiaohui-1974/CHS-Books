# 第8章：曲线曲面积分速算

**学习时间**: 4小时  
**考试频率**: ⭐⭐⭐⭐⭐  
**难度**: ⭐⭐⭐⭐⭐

---

## 一、第一类曲线积分

### 1.1 定义与计算

**定义**（对弧长的曲线积分）：
$$\int_L f(x,y)\mathrm{d}s$$

**参数方程法**：
设曲线$L: \begin{cases} x=\varphi(t) \\ y=\psi(t) \end{cases}, t \in [\alpha, \beta]$

$$\int_L f(x,y)\mathrm{d}s = \int_\alpha^\beta f(\varphi(t),\psi(t))\sqrt{\varphi'^2(t)+\psi'^2(t)}\mathrm{d}t$$

**例题1**：计算$\int_L (x^2+y^2)\mathrm{d}s$，$L: x^2+y^2=R^2$（上半圆）

**解**：
参数化：$x=R\cos t, y=R\sin t, t \in [0,\pi]$

$$\mathrm{d}s = \sqrt{(-R\sin t)^2+(R\cos t)^2}\mathrm{d}t = R\mathrm{d}t$$

$$\int_L (x^2+y^2)\mathrm{d}s = \int_0^\pi R^2 \cdot R\mathrm{d}t = \pi R^3$$

---

## 二、第二类曲线积分

### 2.1 定义与计算

**定义**（对坐标的曲线积分）：
$$\int_L P(x,y)\mathrm{d}x + Q(x,y)\mathrm{d}y$$

**方向**：重要！积分值与路径方向有关

**参数方程法**：
$$\int_L P\mathrm{d}x + Q\mathrm{d}y = \int_\alpha^\beta [P\varphi'(t) + Q\psi'(t)]\mathrm{d}t$$

**例题2**：计算$\int_L x\mathrm{d}x + y\mathrm{d}y$，$L$从$(0,0)$到$(1,1)$沿$y=x^2$

**解**：
参数化：$x=t, y=t^2, t \in [0,1]$

$$\mathrm{d}x=\mathrm{d}t, \quad \mathrm{d}y=2t\mathrm{d}t$$

$$\int_L x\mathrm{d}x + y\mathrm{d}y = \int_0^1 (t \cdot 1 + t^2 \cdot 2t)\mathrm{d}t$$
$$= \int_0^1 (t+2t^3)\mathrm{d}t = \frac{1}{2}+\frac{1}{2} = 1$$

---

### 2.2 格林公式

**条件**：$D$为有界闭区域，$L$为正向边界

**公式**：
$$\oint_L P\mathrm{d}x + Q\mathrm{d}y = \iint_D \left(\frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y}\right)\mathrm{d}x\mathrm{d}y$$

**例题3**：计算$\oint_L x^2\mathrm{d}x + xy\mathrm{d}y$，$L: x^2+y^2=1$（逆时针）

**解**：
$P=x^2, Q=xy$

$$\frac{\partial Q}{\partial x}=y, \quad \frac{\partial P}{\partial y}=0$$

$$\oint_L = \iint_D y\mathrm{d}x\mathrm{d}y$$

极坐标：
$$= \int_0^{2\pi} \int_0^1 r\sin\theta \cdot r\mathrm{d}r\mathrm{d}\theta$$
$$= \int_0^{2\pi} \sin\theta\mathrm{d}\theta \int_0^1 r^2\mathrm{d}r = 0$$

---

### 2.3 路径无关条件

**定理**：在单连通区域$D$内，以下等价：
1. $\int_L P\mathrm{d}x + Q\mathrm{d}y$与路径无关
2. $\oint_L P\mathrm{d}x + Q\mathrm{d}y = 0$
3. $\frac{\partial P}{\partial y} = \frac{\partial Q}{\partial x}$
4. $P\mathrm{d}x + Q\mathrm{d}y$是某函数$u$的全微分

**原函数求法**：
$$u(x,y) = \int_{(x_0,y_0)}^{(x,y)} P\mathrm{d}x + Q\mathrm{d}y$$

**例题4**：验证$(2xy+y^2)\mathrm{d}x + (x^2+2xy)\mathrm{d}y$是全微分，求原函数

**解**：
$P=2xy+y^2, Q=x^2+2xy$

$$\frac{\partial P}{\partial y}=2x+2y, \quad \frac{\partial Q}{\partial x}=2x+2y$$

$\frac{\partial P}{\partial y}=\frac{\partial Q}{\partial x}$，是全微分✓

求原函数：
$$u = \int P\mathrm{d}x = \int (2xy+y^2)\mathrm{d}x = x^2y+xy^2+\varphi(y)$$

$$\frac{\partial u}{\partial y} = x^2+2xy+\varphi'(y) = Q = x^2+2xy$$

$$\varphi'(y)=0 \Rightarrow \varphi(y)=C$$

$$u = x^2y+xy^2+C$$

---

## 三、第一类曲面积分

### 3.1 定义与计算

**定义**（对面积的曲面积分）：
$$\iint_\Sigma f(x,y,z)\mathrm{d}S$$

**计算公式**（投影法）：
设曲面$\Sigma: z=z(x,y)$，投影到$xy$平面为$D_{xy}$

$$\iint_\Sigma f(x,y,z)\mathrm{d}S = \iint_{D_{xy}} f(x,y,z(x,y))\sqrt{1+z_x^2+z_y^2}\mathrm{d}x\mathrm{d}y$$

**例题5**：计算$\iint_\Sigma z\mathrm{d}S$，$\Sigma: z=\sqrt{x^2+y^2}, x^2+y^2 \leq 1$

**解**：
$$z_x = \frac{x}{\sqrt{x^2+y^2}}, \quad z_y = \frac{y}{\sqrt{x^2+y^2}}$$

$$\sqrt{1+z_x^2+z_y^2} = \sqrt{1+\frac{x^2+y^2}{x^2+y^2}} = \sqrt{2}$$

$$\iint_\Sigma z\mathrm{d}S = \iint_{D_{xy}} \sqrt{x^2+y^2} \cdot \sqrt{2}\mathrm{d}x\mathrm{d}y$$

极坐标：
$$= \sqrt{2} \int_0^{2\pi} \int_0^1 r \cdot r\mathrm{d}r\mathrm{d}\theta = \sqrt{2} \cdot 2\pi \cdot \frac{1}{3} = \frac{2\sqrt{2}\pi}{3}$$

---

## 四、第二类曲面积分

### 4.1 定义与计算

**定义**（对坐标的曲面积分）：
$$\iint_\Sigma P\mathrm{d}y\mathrm{d}z + Q\mathrm{d}z\mathrm{d}x + R\mathrm{d}x\mathrm{d}y$$

**方向**：取曲面的外侧或上侧

**计算公式**（投影法）：
$$\iint_\Sigma R\mathrm{d}x\mathrm{d}y = \pm \iint_{D_{xy}} R(x,y,z(x,y))\mathrm{d}x\mathrm{d}y$$

符号：上侧取$+$，下侧取$-$

**例题6**：计算$\iint_\Sigma x\mathrm{d}y\mathrm{d}z + y\mathrm{d}z\mathrm{d}x + z\mathrm{d}x\mathrm{d}y$，
$\Sigma: x^2+y^2+z^2=R^2$（外侧）

**解**（对称性）：
$\Sigma$关于三个坐标平面对称

利用对称性，分别计算：
- $\iint x\mathrm{d}y\mathrm{d}z$：投影到$yz$平面，奇函数，$=0$？不对！

**正确方法**（高斯公式）：
$$\iint_\Sigma = \iiint_\Omega \left(\frac{\partial x}{\partial x}+\frac{\partial y}{\partial y}+\frac{\partial z}{\partial z}\right)\mathrm{d}V$$
$$= \iiint_\Omega 3\mathrm{d}V = 3 \cdot \frac{4\pi R^3}{3} = 4\pi R^3$$

---

### 4.2 高斯公式

**条件**：$\Omega$为空间有界闭区域，$\Sigma$为外侧闭曲面

**公式**（散度定理）：
$$\oiint_\Sigma P\mathrm{d}y\mathrm{d}z + Q\mathrm{d}z\mathrm{d}x + R\mathrm{d}x\mathrm{d}y$$
$$= \iiint_\Omega \left(\frac{\partial P}{\partial x}+\frac{\partial Q}{\partial y}+\frac{\partial R}{\partial z}\right)\mathrm{d}V$$

**散度**：$\nabla \cdot \vec{F} = \frac{\partial P}{\partial x}+\frac{\partial Q}{\partial y}+\frac{\partial R}{\partial z}$

**例题7**：计算$\oiint_\Sigma x^3\mathrm{d}y\mathrm{d}z + y^3\mathrm{d}z\mathrm{d}x + z^3\mathrm{d}x\mathrm{d}y$，
$\Sigma: x^2+y^2+z^2=R^2$（外侧）

**解**：
$$\nabla \cdot \vec{F} = 3x^2+3y^2+3z^2$$

$$\oiint_\Sigma = \iiint_\Omega 3(x^2+y^2+z^2)\mathrm{d}V$$

球坐标：
$$= \int_0^{2\pi} \int_0^\pi \int_0^R 3\rho^2 \cdot \rho^2\sin\varphi\mathrm{d}\rho\mathrm{d}\varphi\mathrm{d}\theta$$
$$= 4\pi \int_0^R 3\rho^4\mathrm{d}\rho = 4\pi \cdot \frac{3R^5}{5} = \frac{12\pi R^5}{5}$$

---

### 4.3 斯托克斯公式

**条件**：$\Sigma$为光滑有向曲面，$\partial\Sigma$为边界曲线（右手法则）

**公式**（旋度定理）：
$$\oint_{\partial\Sigma} P\mathrm{d}x + Q\mathrm{d}y + R\mathrm{d}z$$
$$= \iint_\Sigma \begin{vmatrix}
\mathrm{d}y\mathrm{d}z & \mathrm{d}z\mathrm{d}x & \mathrm{d}x\mathrm{d}y \\
\frac{\partial}{\partial x} & \frac{\partial}{\partial y} & \frac{\partial}{\partial z} \\
P & Q & R
\end{vmatrix}$$

$$= \iint_\Sigma \left(\frac{\partial R}{\partial y}-\frac{\partial Q}{\partial z}\right)\mathrm{d}y\mathrm{d}z + \left(\frac{\partial P}{\partial z}-\frac{\partial R}{\partial x}\right)\mathrm{d}z\mathrm{d}x + \left(\frac{\partial Q}{\partial x}-\frac{\partial P}{\partial y}\right)\mathrm{d}x\mathrm{d}y$$

**旋度**：$\nabla \times \vec{F} = \left(\frac{\partial R}{\partial y}-\frac{\partial Q}{\partial z}, \frac{\partial P}{\partial z}-\frac{\partial R}{\partial x}, \frac{\partial Q}{\partial x}-\frac{\partial P}{\partial y}\right)$

**例题8**：验证$\oint_L y\mathrm{d}x + z\mathrm{d}y + x\mathrm{d}z$，$L: x^2+y^2=1, z=0$（逆时针）

**解**：
取$\Sigma: z=0, x^2+y^2 \leq 1$（上侧）

$P=y, Q=z, R=x$

$$\frac{\partial R}{\partial y}-\frac{\partial Q}{\partial z} = 0-1=-1$$
$$\frac{\partial P}{\partial z}-\frac{\partial R}{\partial x} = 0-1=-1$$
$$\frac{\partial Q}{\partial x}-\frac{\partial P}{\partial y} = 0-1=-1$$

$$\oint_L = \iint_\Sigma (-1)\mathrm{d}y\mathrm{d}z + (-1)\mathrm{d}z\mathrm{d}x + (-1)\mathrm{d}x\mathrm{d}y$$

$z=0$，$\mathrm{d}z=0$，只剩最后一项：
$$= -\iint_{D_{xy}} \mathrm{d}x\mathrm{d}y = -\pi$$

---

## 五、Python实现

```python
import numpy as np
import matplotlib.pyplot as plt
from mpl_toolkits.mplot3d import Axes3D
from scipy import integrate

class CurveAndSurfaceIntegral:
    """曲线曲面积分工具"""
    
    @staticmethod
    def line_integral_first_kind(f, curve, t_range):
        """
        第一类曲线积分
        
        参数:
            f: 被积函数f(x,y)
            curve: 参数方程(x(t), y(t), dx/dt, dy/dt)
            t_range: 参数范围(t_min, t_max)
        """
        x_func, y_func, dxdt_func, dydt_func = curve
        
        def integrand(t):
            x, y = x_func(t), y_func(t)
            dxdt, dydt = dxdt_func(t), dydt_func(t)
            ds = np.sqrt(dxdt**2 + dydt**2)
            return f(x, y) * ds
        
        result, error = integrate.quad(integrand, t_range[0], t_range[1])
        return result
    
    @staticmethod
    def line_integral_second_kind(P, Q, curve, t_range):
        """
        第二类曲线积分
        
        参数:
            P, Q: 函数P(x,y), Q(x,y)
            curve: 参数方程(x(t), y(t), dx/dt, dy/dt)
            t_range: 参数范围
        """
        x_func, y_func, dxdt_func, dydt_func = curve
        
        def integrand(t):
            x, y = x_func(t), y_func(t)
            dxdt, dydt = dxdt_func(t), dydt_func(t)
            return P(x, y) * dxdt + Q(x, y) * dydt
        
        result, error = integrate.quad(integrand, t_range[0], t_range[1])
        return result
    
    @staticmethod
    def check_exact_differential(P, Q, x_range, y_range, h=1e-5):
        """
        检查是否为全微分
        
        返回: (is_exact, max_error)
        """
        x = np.linspace(x_range[0], x_range[1], 20)
        y = np.linspace(y_range[0], y_range[1], 20)
        
        max_error = 0
        
        for xi in x:
            for yi in y:
                dP_dy = (P(xi, yi+h) - P(xi, yi-h)) / (2*h)
                dQ_dx = (Q(xi+h, yi) - Q(xi-h, yi)) / (2*h)
                error = abs(dP_dy - dQ_dx)
                max_error = max(max_error, error)
        
        is_exact = max_error < 1e-4
        return is_exact, max_error
    
    @staticmethod
    def surface_integral_first_kind(f, surface, u_range, v_range):
        """
        第一类曲面积分
        
        参数:
            f: 被积函数f(x,y,z)
            surface: 参数方程(x(u,v), y(u,v), z(u,v))
            u_range, v_range: 参数范围
        """
        x_func, y_func, z_func = surface
        
        def integrand(v, u):
            # 计算切向量
            h = 1e-6
            
            r_u = np.array([
                (x_func(u+h, v) - x_func(u-h, v)) / (2*h),
                (y_func(u+h, v) - y_func(u-h, v)) / (2*h),
                (z_func(u+h, v) - z_func(u-h, v)) / (2*h)
            ])
            
            r_v = np.array([
                (x_func(u, v+h) - x_func(u, v-h)) / (2*h),
                (y_func(u, v+h) - y_func(u, v-h)) / (2*h),
                (z_func(u, v+h) - z_func(u, v-h)) / (2*h)
            ])
            
            # 叉积得法向量模
            dS = np.linalg.norm(np.cross(r_u, r_v))
            
            x, y, z = x_func(u, v), y_func(u, v), z_func(u, v)
            return f(x, y, z) * dS
        
        result, error = integrate.dblquad(
            integrand,
            u_range[0], u_range[1],
            v_range[0], v_range[1]
        )
        return result
    
    @staticmethod
    def plot_curve_3d(curve, t_range, title='空间曲线'):
        """绘制空间曲线"""
        fig = plt.figure(figsize=(10, 8))
        ax = fig.add_subplot(111, projection='3d')
        
        t = np.linspace(t_range[0], t_range[1], 200)
        
        x_func, y_func, z_func = curve[:3]
        
        x = np.array([x_func(ti) for ti in t])
        y = np.array([y_func(ti) for ti in t])
        z = np.array([z_func(ti) for ti in t])
        
        ax.plot(x, y, z, 'b-', linewidth=2.5, label='曲线L')
        
        # 标注起点终点
        ax.scatter([x[0]], [y[0]], [z[0]], c='green', s=100,
                  label='起点', marker='o')
        ax.scatter([x[-1]], [y[-1]], [z[-1]], c='red', s=100,
                  label='终点', marker='s')
        
        # 方向箭头
        n_arrows = 10
        for i in range(0, len(t)-1, len(t)//n_arrows):
            ax.quiver(x[i], y[i], z[i],
                     x[i+1]-x[i], y[i+1]-y[i], z[i+1]-z[i],
                     color='red', arrow_length_ratio=0.3, alpha=0.6)
        
        ax.set_xlabel('x')
        ax.set_ylabel('y')
        ax.set_zlabel('z')
        ax.set_title(title, fontweight='bold')
        ax.legend()
        ax.grid(True, alpha=0.3)
        
        plt.tight_layout()
        plt.savefig('curve_3d.png', dpi=300)
        plt.show()
    
    @staticmethod
    def plot_surface_3d(surface, u_range, v_range, title='曲面'):
        """绘制参数曲面"""
        fig = plt.figure(figsize=(10, 8))
        ax = fig.add_subplot(111, projection='3d')
        
        u = np.linspace(u_range[0], u_range[1], 50)
        v = np.linspace(v_range[0], v_range[1], 50)
        U, V = np.meshgrid(u, v)
        
        x_func, y_func, z_func = surface
        
        X = np.zeros_like(U)
        Y = np.zeros_like(V)
        Z = np.zeros_like(U)
        
        for i in range(U.shape[0]):
            for j in range(U.shape[1]):
                X[i,j] = x_func(U[i,j], V[i,j])
                Y[i,j] = y_func(U[i,j], V[i,j])
                Z[i,j] = z_func(U[i,j], V[i,j])
        
        surf = ax.plot_surface(X, Y, Z, cmap='viridis', alpha=0.8,
                              edgecolor='black', linewidth=0.3)
        
        ax.set_xlabel('x')
        ax.set_ylabel('y')
        ax.set_zlabel('z')
        ax.set_title(title, fontweight='bold')
        fig.colorbar(surf, ax=ax, shrink=0.5)
        
        plt.tight_layout()
        plt.savefig('surface_3d.png', dpi=300)
        plt.show()

# 示例使用
print("="*60)
print("曲线曲面积分计算器")
print("="*60)

csi = CurveAndSurfaceIntegral()

# 例题1：第一类曲线积分
print("\n例题1：∫_L (x²+y²)ds, L: x²+y²=R² (上半圆), R=2")

R = 2
curve1 = (
    lambda t: R * np.cos(t),  # x(t)
    lambda t: R * np.sin(t),  # y(t)
    lambda t: -R * np.sin(t), # dx/dt
    lambda t: R * np.cos(t)   # dy/dt
)

def f1(x, y):
    return x**2 + y**2

result1 = csi.line_integral_first_kind(f1, curve1, (0, np.pi))
theoretical1 = np.pi * R**3

print(f"  数值结果 = {result1:.10f}")
print(f"  理论结果 = {theoretical1:.10f}")
print(f"  误差 = {abs(result1 - theoretical1):.2e}")

# 例题2：第二类曲线积分
print("\n例题2：∫_L xdx + ydy, L: (0,0)→(1,1) 沿y=x²")

curve2 = (
    lambda t: t,      # x(t)
    lambda t: t**2,   # y(t)
    lambda t: 1,      # dx/dt
    lambda t: 2*t     # dy/dt
)

def P2(x, y):
    return x

def Q2(x, y):
    return y

result2 = csi.line_integral_second_kind(P2, Q2, curve2, (0, 1))
print(f"  数值结果 = {result2:.10f}")
print(f"  理论结果 = 1.0")

# 检查全微分
print("\n例题3：检查(2xy+y²)dx + (x²+2xy)dy是否为全微分")

def P3(x, y):
    return 2*x*y + y**2

def Q3(x, y):
    return x**2 + 2*x*y

is_exact, error = csi.check_exact_differential(P3, Q3, (-2, 2), (-2, 2))
print(f"  是否为全微分: {is_exact}")
print(f"  最大误差: {error:.2e}")

print("\n曲线曲面积分计算完成！")
```

---

**本章重点**：
- 第一类/第二类曲线积分
- 格林公式与路径无关
- 第一类/第二类曲面积分
- 高斯公式与斯托克斯公式
- 散度与旋度概念

**全书完成！** 🎉
